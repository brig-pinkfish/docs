---
title: "vercel-environment"
sidebarTitle: "vercel-environment"
description: "Vercel Environment"
---

{/* Auto-generated by scripts/generate-application-mcp-docs.ts — do not edit manually. To regenerate: see docs/scripts/README.md */}

**Server path:** `/vercel-environment` | **Type:** Application | **PCID required:** Yes

## Tools

| Tool | Description |
| --- | --- |
| [`vercel_environment_create_custom_environment`](#vercel_environment_create_custom_environment) | Create a custom environment for the current project. |
| [`vercel_environment_create_edge_config`](#vercel_environment_create_edge_config) | Create an Edge Config |
| [`vercel_environment_create_edge_config_token`](#vercel_environment_create_edge_config_token) | Create an Edge Config token |
| [`vercel_environment_create_shared_env_variable`](#vercel_environment_create_shared_env_variable) | Create one or more shared environment variables |
| [`vercel_environment_dangerously_delete_by_src_images`](#vercel_environment_dangerously_delete_by_src_images) | Dangerously delete by source image |
| [`vercel_environment_dangerously_delete_by_tags`](#vercel_environment_dangerously_delete_by_tags) | Dangerously delete by tag |
| [`vercel_environment_delete_edge_config`](#vercel_environment_delete_edge_config) | Delete an Edge Config |
| [`vercel_environment_delete_edge_config_schema`](#vercel_environment_delete_edge_config_schema) | Delete an Edge Config's schema |
| [`vercel_environment_delete_edge_config_tokens`](#vercel_environment_delete_edge_config_tokens) | Delete one or more Edge Config tokens |
| [`vercel_environment_delete_shared_env_variable`](#vercel_environment_delete_shared_env_variable) | Delete one or more Env Var |
| [`vercel_environment_get_custom_environment`](#vercel_environment_get_custom_environment) | Retrieve a custom environment |
| [`vercel_environment_get_edge_config`](#vercel_environment_get_edge_config) | Get an Edge Config |
| [`vercel_environment_get_edge_config_backup`](#vercel_environment_get_edge_config_backup) | Get Edge Config backup |
| [`vercel_environment_get_edge_config_backups`](#vercel_environment_get_edge_config_backups) | Get Edge Config backups |
| [`vercel_environment_get_edge_config_item`](#vercel_environment_get_edge_config_item) | Get an Edge Config item |
| [`vercel_environment_get_edge_config_items`](#vercel_environment_get_edge_config_items) | Get Edge Config items |
| [`vercel_environment_get_edge_config_schema`](#vercel_environment_get_edge_config_schema) | Get Edge Config schema |
| [`vercel_environment_get_edge_config_token`](#vercel_environment_get_edge_config_token) | Get Edge Config token meta data |
| [`vercel_environment_get_edge_config_tokens`](#vercel_environment_get_edge_config_tokens) | Get all tokens of an Edge Config |
| [`vercel_environment_get_edge_configs`](#vercel_environment_get_edge_configs) | Get Edge Configs |
| [`vercel_environment_get_projects_by_id_or_name_custom_environments`](#vercel_environment_get_projects_by_id_or_name_custom_environments) | Retrieve custom environments |
| [`vercel_environment_get_shared_env_var`](#vercel_environment_get_shared_env_var) | Retrieve the decrypted value of a Shared Environment Variable by id. |
| [`vercel_environment_invalidate_by_src_images`](#vercel_environment_invalidate_by_src_images) | Invalidate by source image |
| [`vercel_environment_invalidate_by_tags`](#vercel_environment_invalidate_by_tags) | Invalidate by tag |
| [`vercel_environment_list_shared_env_variable`](#vercel_environment_list_shared_env_variable) | Lists all Shared Environment Variables for a team |
| [`vercel_environment_patch_edge_config_items`](#vercel_environment_patch_edge_config_items) | Update Edge Config items in batch |
| [`vercel_environment_patch_edge_config_schema`](#vercel_environment_patch_edge_config_schema) | Update Edge Config schema |
| [`vercel_environment_remove_custom_environment`](#vercel_environment_remove_custom_environment) | Remove a custom environment |
| [`vercel_environment_unlink_shared_env_variable`](#vercel_environment_unlink_shared_env_variable) | Disconnects a shared environment variable for a given project |
| [`vercel_environment_update_custom_environment`](#vercel_environment_update_custom_environment) | Update a custom environment |
| [`vercel_environment_update_edge_config`](#vercel_environment_update_edge_config) | Update an Edge Config |
| [`vercel_environment_update_shared_env_variable`](#vercel_environment_update_shared_env_variable) | Updates one or more shared environment variables |

---

## vercel_environment_create_custom_environment

Create a custom environment for the current project.

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `idOrName` | string | Yes | — | The unique project identifier or the project name |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `branchMatcher` | object | No | — | How we want to determine a matching branch. This is optional. |
| `copyEnvVarsFrom` | string | No | — | Where to copy environment variables from. This is optional. |
| `description` | string | No | — | Description of the custom environment. This is optional. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "idOrName": {
      "type": "string",
      "description": "The unique project identifier or the project name"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "branchMatcher": {
      "type": "object",
      "description": "How we want to determine a matching branch. This is optional.",
      "properties": {
        "type": {
          "description": "Type of matcher. One of \\\"equals\\\", \\\"startsWith\\\", or \\\"endsWith\\\"."
        },
        "pattern": {
          "type": "string",
          "description": "Git branch name or portion thereof."
        }
      },
      "required": [
        "type",
        "pattern"
      ]
    },
    "copyEnvVarsFrom": {
      "type": "string",
      "description": "Where to copy environment variables from. This is optional."
    },
    "description": {
      "type": "string",
      "description": "Description of the custom environment. This is optional."
    }
  },
  "required": [
    "PCID",
    "idOrName"
  ]
}
```

</Expandable>

---

## vercel_environment_create_edge_config

Create an Edge Config

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | Yes | — | The Team slug to perform the request on behalf of. |
| `items` | object | No | — | items |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "items": {
      "type": "object",
      "description": "items"
    }
  },
  "required": [
    "PCID",
    "slug"
  ]
}
```

</Expandable>

---

## vercel_environment_create_edge_config_token

Create an Edge Config token

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `label` | string | Yes | — | label |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "label": {
      "type": "string",
      "description": "label"
    }
  },
  "required": [
    "PCID",
    "edgeConfigId",
    "label"
  ]
}
```

</Expandable>

---

## vercel_environment_create_shared_env_variable

Create one or more shared environment variables

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `evs` | any[] | Yes | — | evs |
| `projectId` | any[] | No | — | Associate a Shared Environment Variable to projects. |
| `target` | any[] | No | — | The target environment of the Shared Environment Variable |
| `type` | string | No | — | The type of environment variable |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "evs": {
      "type": "array",
      "description": "evs"
    },
    "projectId": {
      "type": "array",
      "description": "Associate a Shared Environment Variable to projects."
    },
    "target": {
      "type": "array",
      "description": "The target environment of the Shared Environment Variable"
    },
    "type": {
      "type": "string",
      "description": "The type of environment variable",
      "enum": [
        "encrypted",
        "sensitive"
      ]
    }
  },
  "required": [
    "PCID",
    "evs"
  ]
}
```

</Expandable>

---

## vercel_environment_dangerously_delete_by_src_images

Dangerously delete by source image

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `projectIdOrName` | string | Yes | — | projectIdOrName |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `revalidationDeadlineSeconds` | number | No | — | revalidationDeadlineSeconds |
| `srcImages` | any[] | Yes | — | srcImages |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "projectIdOrName": {
      "type": "string",
      "description": "projectIdOrName"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "revalidationDeadlineSeconds": {
      "type": "number",
      "description": "revalidationDeadlineSeconds"
    },
    "srcImages": {
      "type": "array",
      "description": "srcImages"
    }
  },
  "required": [
    "PCID",
    "projectIdOrName",
    "srcImages"
  ]
}
```

</Expandable>

---

## vercel_environment_dangerously_delete_by_tags

Dangerously delete by tag

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `projectIdOrName` | string | Yes | — | projectIdOrName |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `revalidationDeadlineSeconds` | number | No | — | revalidationDeadlineSeconds |
| `tags` | any[] | Yes | — | tags |
| `target` | string | No | — | target |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "projectIdOrName": {
      "type": "string",
      "description": "projectIdOrName"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "revalidationDeadlineSeconds": {
      "type": "number",
      "description": "revalidationDeadlineSeconds"
    },
    "tags": {
      "type": "array",
      "description": "tags"
    },
    "target": {
      "type": "string",
      "description": "target",
      "enum": [
        "production",
        "preview"
      ]
    }
  },
  "required": [
    "PCID",
    "projectIdOrName",
    "tags"
  ]
}
```

</Expandable>

---

## vercel_environment_delete_edge_config

Delete an Edge Config

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId"
  ]
}
```

</Expandable>

---

## vercel_environment_delete_edge_config_schema

Delete an Edge Config's schema

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId"
  ]
}
```

</Expandable>

---

## vercel_environment_delete_edge_config_tokens

Delete one or more Edge Config tokens

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `tokens` | any[] | Yes | — | tokens |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "tokens": {
      "type": "array",
      "description": "tokens"
    }
  },
  "required": [
    "PCID",
    "edgeConfigId",
    "tokens"
  ]
}
```

</Expandable>

---

## vercel_environment_delete_shared_env_variable

Delete one or more Env Var

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `ids` | any[] | Yes | — | IDs of the Shared Environment Variables to delete |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "ids": {
      "type": "array",
      "description": "IDs of the Shared Environment Variables to delete"
    }
  },
  "required": [
    "PCID",
    "ids"
  ]
}
```

</Expandable>

---

## vercel_environment_get_custom_environment

Retrieve a custom environment

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `idOrName` | string | Yes | — | The unique project identifier or the project name |
| `environmentSlugOrId` | string | Yes | — | The unique custom environment identifier within the project |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "idOrName": {
      "type": "string",
      "description": "The unique project identifier or the project name"
    },
    "environmentSlugOrId": {
      "type": "string",
      "description": "The unique custom environment identifier within the project"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "idOrName",
    "environmentSlugOrId"
  ]
}
```

</Expandable>

---

## vercel_environment_get_edge_config

Get an Edge Config

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId"
  ]
}
```

</Expandable>

---

## vercel_environment_get_edge_config_backup

Get Edge Config backup

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `edgeConfigBackupVersionId` | string | Yes | — | edgeConfigBackupVersionId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "edgeConfigBackupVersionId": {
      "type": "string",
      "description": "edgeConfigBackupVersionId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId",
    "edgeConfigBackupVersionId"
  ]
}
```

</Expandable>

---

## vercel_environment_get_edge_config_backups

Get Edge Config backups

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `next` | string | No | — | next |
| `limit` | number | No | — | limit |
| `metadata` | string | No | — | metadata |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "next": {
      "type": "string",
      "description": "next"
    },
    "limit": {
      "type": "number",
      "description": "limit"
    },
    "metadata": {
      "type": "string",
      "description": "metadata"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId"
  ]
}
```

</Expandable>

---

## vercel_environment_get_edge_config_item

Get an Edge Config item

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `edgeConfigItemKey` | string | Yes | — | edgeConfigItemKey |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "edgeConfigItemKey": {
      "type": "string",
      "description": "edgeConfigItemKey"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId",
    "edgeConfigItemKey"
  ]
}
```

</Expandable>

---

## vercel_environment_get_edge_config_items

Get Edge Config items

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId"
  ]
}
```

</Expandable>

---

## vercel_environment_get_edge_config_schema

Get Edge Config schema

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId"
  ]
}
```

</Expandable>

---

## vercel_environment_get_edge_config_token

Get Edge Config token meta data

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `token` | string | Yes | — | token |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "token": {
      "type": "string",
      "description": "token"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId",
    "token"
  ]
}
```

</Expandable>

---

## vercel_environment_get_edge_config_tokens

Get all tokens of an Edge Config

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId"
  ]
}
```

</Expandable>

---

## vercel_environment_get_edge_configs

Get Edge Configs

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID"
  ]
}
```

</Expandable>

---

## vercel_environment_get_projects_by_id_or_name_custom_environments

Retrieve custom environments

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `idOrName` | string | Yes | — | The unique project identifier or the project name |
| `gitBranch` | string | No | — | Fetch custom environments for a specific git branch |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "idOrName": {
      "type": "string",
      "description": "The unique project identifier or the project name"
    },
    "gitBranch": {
      "type": "string",
      "description": "Fetch custom environments for a specific git branch"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "idOrName"
  ]
}
```

</Expandable>

---

## vercel_environment_get_shared_env_var

Retrieve the decrypted value of a Shared Environment Variable by id.

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | string | Yes | — | The unique ID for the Shared Environment Variable to get the decrypted value. |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "id": {
      "type": "string",
      "description": "The unique ID for the Shared Environment Variable to get the decrypted value."
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "id"
  ]
}
```

</Expandable>

---

## vercel_environment_invalidate_by_src_images

Invalidate by source image

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `projectIdOrName` | string | Yes | — | projectIdOrName |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `srcImages` | any[] | Yes | — | srcImages |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "projectIdOrName": {
      "type": "string",
      "description": "projectIdOrName"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "srcImages": {
      "type": "array",
      "description": "srcImages"
    }
  },
  "required": [
    "PCID",
    "projectIdOrName",
    "srcImages"
  ]
}
```

</Expandable>

---

## vercel_environment_invalidate_by_tags

Invalidate by tag

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `projectIdOrName` | string | Yes | — | projectIdOrName |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `tags` | any[] | Yes | — | tags |
| `target` | string | No | — | target |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "projectIdOrName": {
      "type": "string",
      "description": "projectIdOrName"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "tags": {
      "type": "array",
      "description": "tags"
    },
    "target": {
      "type": "string",
      "description": "target",
      "enum": [
        "production",
        "preview"
      ]
    }
  },
  "required": [
    "PCID",
    "projectIdOrName",
    "tags"
  ]
}
```

</Expandable>

---

## vercel_environment_list_shared_env_variable

Lists all Shared Environment Variables for a team

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `search` | string | No | — | search |
| `projectId` | string | No | — | Filter SharedEnvVariables that belong to a project |
| `ids` | string | No | — | Filter SharedEnvVariables based on comma separated ids |
| `exclude_ids` | string | No | — | Filter SharedEnvVariables based on comma separated ids |
| `exclude-ids` | string | No | — | Filter SharedEnvVariables based on comma separated ids |
| `exclude_projectId` | string | No | — | Filter SharedEnvVariables that belong to a project |
| `exclude-projectId` | string | No | — | Filter SharedEnvVariables that belong to a project |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "search": {
      "type": "string",
      "description": "search"
    },
    "projectId": {
      "type": "string",
      "description": "Filter SharedEnvVariables that belong to a project"
    },
    "ids": {
      "type": "string",
      "description": "Filter SharedEnvVariables based on comma separated ids"
    },
    "exclude_ids": {
      "type": "string",
      "description": "Filter SharedEnvVariables based on comma separated ids"
    },
    "exclude-ids": {
      "type": "string",
      "description": "Filter SharedEnvVariables based on comma separated ids"
    },
    "exclude_projectId": {
      "type": "string",
      "description": "Filter SharedEnvVariables that belong to a project"
    },
    "exclude-projectId": {
      "type": "string",
      "description": "Filter SharedEnvVariables that belong to a project"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID"
  ]
}
```

</Expandable>

---

## vercel_environment_patch_edge_config_items

Update Edge Config items in batch

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `items` | any[] | Yes | — | items |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "items": {
      "type": "array",
      "description": "items"
    }
  },
  "required": [
    "PCID",
    "edgeConfigId",
    "items"
  ]
}
```

</Expandable>

---

## vercel_environment_patch_edge_config_schema

Update Edge Config schema

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `dryRun` | string | No | — | dryRun |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `definition` | object | Yes | — | definition |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "dryRun": {
      "type": "string",
      "description": "dryRun"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "definition": {
      "description": "definition"
    }
  },
  "required": [
    "PCID",
    "edgeConfigId",
    "definition"
  ]
}
```

</Expandable>

---

## vercel_environment_remove_custom_environment

Remove a custom environment

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `idOrName` | string | Yes | — | The unique project identifier or the project name |
| `environmentSlugOrId` | string | Yes | — | The unique custom environment identifier within the project |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `deleteUnassignedEnvironmentVariables` | boolean | No | — | Delete Environment Variables that are not assigned to any environments. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "idOrName": {
      "type": "string",
      "description": "The unique project identifier or the project name"
    },
    "environmentSlugOrId": {
      "type": "string",
      "description": "The unique custom environment identifier within the project"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "deleteUnassignedEnvironmentVariables": {
      "type": "boolean",
      "description": "Delete Environment Variables that are not assigned to any environments."
    }
  },
  "required": [
    "PCID",
    "idOrName",
    "environmentSlugOrId"
  ]
}
```

</Expandable>

---

## vercel_environment_unlink_shared_env_variable

Disconnects a shared environment variable for a given project

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `id` | string | Yes | — | The unique ID for the Shared Environment Variable to unlink from the project. |
| `projectId` | string | Yes | — | projectId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "id": {
      "type": "string",
      "description": "The unique ID for the Shared Environment Variable to unlink from the project."
    },
    "projectId": {
      "type": "string",
      "description": "projectId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "id",
    "projectId"
  ]
}
```

</Expandable>

---

## vercel_environment_update_custom_environment

Update a custom environment

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `idOrName` | string | Yes | — | The unique project identifier or the project name |
| `environmentSlugOrId` | string | Yes | — | The unique custom environment identifier within the project |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `branchMatcher` | object | No | — | How we want to determine a matching branch. This is optional. |
| `description` | string | No | — | Description of the custom environment. This is optional. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "idOrName": {
      "type": "string",
      "description": "The unique project identifier or the project name"
    },
    "environmentSlugOrId": {
      "type": "string",
      "description": "The unique custom environment identifier within the project"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "branchMatcher": {
      "type": "object",
      "description": "How we want to determine a matching branch. This is optional.",
      "properties": {
        "type": {
          "description": "Type of matcher. One of \\\"equals\\\", \\\"startsWith\\\", or \\\"endsWith\\\"."
        },
        "pattern": {
          "type": "string",
          "description": "Git branch name or portion thereof."
        }
      },
      "required": [
        "type",
        "pattern"
      ]
    },
    "description": {
      "type": "string",
      "description": "Description of the custom environment. This is optional."
    }
  },
  "required": [
    "PCID",
    "idOrName",
    "environmentSlugOrId"
  ]
}
```

</Expandable>

---

## vercel_environment_update_edge_config

Update an Edge Config

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `edgeConfigId` | string | Yes | — | edgeConfigId |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | Yes | — | The Team slug to perform the request on behalf of. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "edgeConfigId": {
      "type": "string",
      "description": "edgeConfigId"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    }
  },
  "required": [
    "PCID",
    "edgeConfigId",
    "slug"
  ]
}
```

</Expandable>

---

## vercel_environment_update_shared_env_variable

Updates one or more shared environment variables

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `teamId` | string | No | — | The Team identifier to perform the request on behalf of. |
| `slug` | string | No | — | The Team slug to perform the request on behalf of. |
| `updates` | object | Yes | — | An object where each key is an environment variable ID (not the key name) and the value is the update to apply |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "teamId": {
      "type": "string",
      "description": "The Team identifier to perform the request on behalf of."
    },
    "slug": {
      "type": "string",
      "description": "The Team slug to perform the request on behalf of."
    },
    "updates": {
      "type": "object",
      "description": "An object where each key is an environment variable ID (not the key name) and the value is the update to apply"
    }
  },
  "required": [
    "PCID",
    "updates"
  ]
}
```

</Expandable>
