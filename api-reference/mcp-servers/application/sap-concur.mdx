---
title: "sap-concur"
sidebarTitle: "sap-concur"
description: "SAP Concur Expenses"
---

{/* Auto-generated by scripts/generate-application-mcp-docs.ts — do not edit manually. To regenerate: see docs/scripts/README.md */}

**Server path:** `/sap-concur` | **Type:** Application | **PCID required:** Yes

## Tools

| Tool | Description |
| --- | --- |
| [`sap_concur_approve_report`](#sap_concur_approve_report) | Approves the specified report |
| [`sap_concur_approve_report_as_system`](#sap_concur_approve_report_as_system) | Approves the specified report using system user |
| [`sap_concur_create_report_comments`](#sap_concur_create_report_comments) | Creates a comment for the specified report |
| [`sap_concur_create_report_comments_with_company_jwt`](#sap_concur_create_report_comments_with_company_jwt) | Creates a comment for the specified report using system user |
| [`sap_concur_create_report_entry_comments`](#sap_concur_create_report_entry_comments) | Creates a comment for the specified expense |
| [`sap_concur_create_report_entry_comments_with_company_jwt`](#sap_concur_create_report_entry_comments_with_company_jwt) | Creates a comment for the specified expense using system user |
| [`sap_concur_delete_allocation_exception`](#sap_concur_delete_allocation_exception) | Deletes an exception on the specified allocation |
| [`sap_concur_delete_attendees`](#sap_concur_delete_attendees) | Removes expense attendees |
| [`sap_concur_delete_report_comments`](#sap_concur_delete_report_comments) | Deletes the comment for the specified report |
| [`sap_concur_delete_report_comments_with_company_jwt`](#sap_concur_delete_report_comments_with_company_jwt) | Deletes the comment for the specified report using system user |
| [`sap_concur_delete_report_entry_comments`](#sap_concur_delete_report_entry_comments) | Deletes the comment for the specified expense |
| [`sap_concur_delete_report_entry_comments_with_company_jwt`](#sap_concur_delete_report_entry_comments_with_company_jwt) | Deletes the comment for the specified expense using system user |
| [`sap_concur_delete_report_entry_exception`](#sap_concur_delete_report_entry_exception) | Deletes an exception on the specified expense |
| [`sap_concur_delete_report_exception`](#sap_concur_delete_report_exception) | Deletes an exception on the specified report |
| [`sap_concur_get_allocation_by_id`](#sap_concur_get_allocation_by_id) | Gets specified allocation for the specified report. |
| [`sap_concur_get_allocation_exceptions`](#sap_concur_get_allocation_exceptions) | Get exceptions for the specified allocation |
| [`sap_concur_get_allocation_exceptions_trusted_or_company_jwt`](#sap_concur_get_allocation_exceptions_trusted_or_company_jwt) | Get exceptions for the specified allocation using system user |
| [`sap_concur_get_allocations`](#sap_concur_get_allocations) | Gets all allocations for the specified expense |
| [`sap_concur_get_expense_attendees`](#sap_concur_get_expense_attendees) | Gets expense attendee associations |
| [`sap_concur_get_expense_attendees_trusted_or_company_jwt`](#sap_concur_get_expense_attendees_trusted_or_company_jwt) | Gets expense attendee associations using system user |
| [`sap_concur_get_report_comments`](#sap_concur_get_report_comments) | Gets comments for the specified report |
| [`sap_concur_get_report_comments_trusted_or_company_jwt`](#sap_concur_get_report_comments_trusted_or_company_jwt) | Gets comments for the specified report using system user |
| [`sap_concur_get_report_entry_comments`](#sap_concur_get_report_entry_comments) | Gets comments for the specified expense |
| [`sap_concur_get_report_entry_comments_trusted_or_company_jwt`](#sap_concur_get_report_entry_comments_trusted_or_company_jwt) | Gets comments for the specified expense using system user |
| [`sap_concur_get_report_entry_exceptions`](#sap_concur_get_report_entry_exceptions) | Get exceptions for the specified expense |
| [`sap_concur_get_report_entry_exceptions_trusted_or_company_jwt`](#sap_concur_get_report_entry_exceptions_trusted_or_company_jwt) | Get exceptions for the specified expense using system user |
| [`sap_concur_get_report_exceptions`](#sap_concur_get_report_exceptions) | Get exceptions for the specified report |
| [`sap_concur_get_report_exceptions_trusted_or_company_jwt`](#sap_concur_get_report_exceptions_trusted_or_company_jwt) | Get exceptions for the specified report using system user |
| [`sap_concur_get_report_expense_detail`](#sap_concur_get_report_expense_detail) | Gets expense details for the specified expense |
| [`sap_concur_get_report_expense_itemizations_summary`](#sap_concur_get_report_expense_itemizations_summary) | Gets a list of itemization summaries for the specified expense |
| [`sap_concur_get_report_expenses_summary`](#sap_concur_get_report_expenses_summary) | Gets all expense summaries for the specified report |
| [`sap_concur_get_reports_to_approve`](#sap_concur_get_reports_to_approve) | Gets the reports to approve. |
| [`sap_concur_get_request_associations`](#sap_concur_get_request_associations) | Gets the requests associated with the specified report |
| [`sap_concur_post_expense_attendees`](#sap_concur_post_expense_attendees) | Associates a set of attendees to a specified expense |
| [`sap_concur_post_new_report`](#sap_concur_post_new_report) | Creates a new report |
| [`sap_concur_recall_report`](#sap_concur_recall_report) | Recalls the specified report |
| [`sap_concur_save_allocation_exception`](#sap_concur_save_allocation_exception) | Adds an exception on the specified allocation |
| [`sap_concur_save_report_entry_exception`](#sap_concur_save_report_entry_exception) | Adds an exception on the specified expense |
| [`sap_concur_save_report_exception`](#sap_concur_save_report_exception) | Adds an exception on the specified report |
| [`sap_concur_send_back_report`](#sap_concur_send_back_report) | Sends back the specified report |
| [`sap_concur_send_back_report_as_system`](#sap_concur_send_back_report_as_system) | Sends back the specified report using system user |
| [`sap_concur_update_report`](#sap_concur_update_report) | Updates the report header |
| [`sap_concur_update_report_allocation`](#sap_concur_update_report_allocation) | Updates a specified allocation |
| [`sap_concur_update_report_comments`](#sap_concur_update_report_comments) | Updates a comment for the specified report |
| [`sap_concur_update_report_comments_with_company_jwt`](#sap_concur_update_report_comments_with_company_jwt) | Updates a comment for the specified report using system user |
| [`sap_concur_update_report_entry_comments`](#sap_concur_update_report_entry_comments) | Updates a comment for the specified expense |
| [`sap_concur_update_report_entry_comments_with_company_jwt`](#sap_concur_update_report_entry_comments_with_company_jwt) | Updates a comment for the specified expense using system user |
| [`sap_concur_update_report_expense_using_short_url`](#sap_concur_update_report_expense_using_short_url) | Updates (limited fields) the specified expense in an un-submitted or submitted report |

---

## sap_concur_approve_report

Approves the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `comment` | string | No | — | This is the approver's remark on the approval action |
| `expenseRejectedComment` | string | No | — | This comment identifies why some of the expenses are rejected on this report. |
| `statusId` | string | No | — | Status that will be assigned to the report by this workflow transition. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "MANAGER"
      ]
    },
    "comment": {
      "type": "string",
      "description": "This is the approver's remark on the approval action"
    },
    "expenseRejectedComment": {
      "type": "string",
      "description": "This comment identifies why some of the expenses are rejected on this report."
    },
    "statusId": {
      "type": "string",
      "description": "Status that will be assigned to the report by this workflow transition."
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_approve_report_as_system

Approves the specified report using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `comment` | string | No | — | This is the approver's remark on the approval action |
| `expectedWorkflowStepInstanceId` | string | No | — | The ID of the workflow step in which this report is expected to be in.This parameter can be used to verify that the report is still in the expected workflow step. |
| `expenseRejectedComment` | string | No | — | This comment identifies why some of the expenses are rejected on this report. |
| `statusId` | string | No | — | Status that will be assigned to the report by this workflow transition. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "comment": {
      "type": "string",
      "description": "This is the approver's remark on the approval action"
    },
    "expectedWorkflowStepInstanceId": {
      "type": "string",
      "description": "The ID of the workflow step in which this report is expected to be in.This parameter can be used to verify that the report is still in the expected workflow step."
    },
    "expenseRejectedComment": {
      "type": "string",
      "description": "This comment identifies why some of the expenses are rejected on this report."
    },
    "statusId": {
      "type": "string",
      "description": "Status that will be assigned to the report by this workflow transition."
    }
  },
  "required": [
    "PCID",
    "reportId"
  ]
}
```

</Expandable>

---

## sap_concur_create_report_comments

Creates a comment for the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `comment` | string | Yes | — | The comments input on the report/expense by all users |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    },
    "comment": {
      "type": "string",
      "description": "The comments input on the report/expense by all users"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType",
    "comment"
  ]
}
```

</Expandable>

---

## sap_concur_create_report_comments_with_company_jwt

Creates a comment for the specified report using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `comment` | string | Yes | — | The comments input on the report/expense by all users |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "comment": {
      "type": "string",
      "description": "The comments input on the report/expense by all users"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "comment"
  ]
}
```

</Expandable>

---

## sap_concur_create_report_entry_comments

Creates a comment for the specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `comment` | string | Yes | — | The comments input on the report/expense by all users |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    },
    "comment": {
      "type": "string",
      "description": "The comments input on the report/expense by all users"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType",
    "comment"
  ]
}
```

</Expandable>

---

## sap_concur_create_report_entry_comments_with_company_jwt

Creates a comment for the specified expense using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `comment` | string | Yes | — | The comments input on the report/expense by all users |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "comment": {
      "type": "string",
      "description": "The comments input on the report/expense by all users"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "comment"
  ]
}
```

</Expandable>

---

## sap_concur_delete_allocation_exception

Deletes an exception on the specified allocation

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `allocationId` | string | Yes | — | The unique identifier of the expense allocation |
| `exceptionCode` | string | Yes | — | The code for the exception |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "allocationId": {
      "type": "string",
      "description": "The unique identifier of the expense allocation"
    },
    "exceptionCode": {
      "type": "string",
      "description": "The code for the exception"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "allocationId",
    "exceptionCode"
  ]
}
```

</Expandable>

---

## sap_concur_delete_attendees

Removes expense attendees

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report that contains the expense with attendees |
| `expenseId` | string | Yes | — | The unique identifier of the expense with associated attendees |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report that contains the expense with attendees"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense with associated attendees"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_delete_report_comments

Deletes the comment for the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_delete_report_comments_with_company_jwt

Deletes the comment for the specified report using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    }
  },
  "required": [
    "PCID",
    "reportId"
  ]
}
```

</Expandable>

---

## sap_concur_delete_report_entry_comments

Deletes the comment for the specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_delete_report_entry_comments_with_company_jwt

Deletes the comment for the specified expense using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId"
  ]
}
```

</Expandable>

---

## sap_concur_delete_report_entry_exception

Deletes an exception on the specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `exceptionCode` | string | Yes | — | The code for the exception |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "exceptionCode": {
      "type": "string",
      "description": "The code for the exception"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "exceptionCode"
  ]
}
```

</Expandable>

---

## sap_concur_delete_report_exception

Deletes an exception on the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `exceptionCode` | string | Yes | — | The code for the exception |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "exceptionCode": {
      "type": "string",
      "description": "The code for the exception"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "exceptionCode"
  ]
}
```

</Expandable>

---

## sap_concur_get_allocation_by_id

Gets specified allocation for the specified report.

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `allocationId` | string | Yes | — | The unique identifier of the expense allocation |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "allocationId": {
      "type": "string",
      "description": "The unique identifier of the expense allocation"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "allocationId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_allocation_exceptions

Get exceptions for the specified allocation

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report that contains the expense associated with the allocations |
| `expenseId` | string | Yes | — | The unique identifier of the expense associated with the allocations |
| `allocationId` | string | Yes | — | The unique identifier of the expense allocation |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report that contains the expense associated with the allocations"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense associated with the allocations"
    },
    "allocationId": {
      "type": "string",
      "description": "The unique identifier of the expense allocation"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "allocationId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_allocation_exceptions_trusted_or_company_jwt

Get exceptions for the specified allocation using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report that contains the expense associated with the allocations |
| `expenseId` | string | Yes | — | The unique identifier of the expense associated with the allocations |
| `allocationId` | string | Yes | — | The unique identifier of the expense allocation |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report that contains the expense associated with the allocations"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense associated with the allocations"
    },
    "allocationId": {
      "type": "string",
      "description": "The unique identifier of the expense allocation"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "allocationId"
  ]
}
```

</Expandable>

---

## sap_concur_get_allocations

Gets all allocations for the specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_expense_attendees

Gets expense attendee associations

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_expense_attendees_trusted_or_company_jwt

Gets expense attendee associations using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_comments

Gets comments for the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `includeAllComments` | string | No | — | Return all the comments that are present on the report at all levels |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "includeAllComments": {
      "type": "string",
      "description": "Return all the comments that are present on the report at all levels"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_comments_trusted_or_company_jwt

Gets comments for the specified report using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `includeAllComments` | string | No | — | Return all the comments that are present on the report at all levels |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "includeAllComments": {
      "type": "string",
      "description": "Return all the comments that are present on the report at all levels"
    }
  },
  "required": [
    "PCID",
    "reportId"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_entry_comments

Gets comments for the specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_entry_comments_trusted_or_company_jwt

Gets comments for the specified expense using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_entry_exceptions

Get exceptions for the specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `excludeItemizations` | string | No | — | Include only exceptions for non-itemized and parent expenses |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "excludeItemizations": {
      "type": "string",
      "description": "Include only exceptions for non-itemized and parent expenses"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_entry_exceptions_trusted_or_company_jwt

Get exceptions for the specified expense using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `excludeItemizations` | string | No | — | Include only exceptions for non-itemized and parent expenses |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "excludeItemizations": {
      "type": "string",
      "description": "Include only exceptions for non-itemized and parent expenses"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_exceptions

Get exceptions for the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `excludeExpenses` | string | No | — | Include only exceptions for the report header |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "excludeExpenses": {
      "type": "string",
      "description": "Include only exceptions for the report header"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_exceptions_trusted_or_company_jwt

Get exceptions for the specified report using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `excludeExpenses` | string | No | — | Include only exceptions for the report header |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "excludeExpenses": {
      "type": "string",
      "description": "Include only exceptions for the report header"
    }
  },
  "required": [
    "PCID",
    "reportId"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_expense_detail

Gets expense details for the specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_expense_itemizations_summary

Gets a list of itemization summaries for the specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_report_expenses_summary

Gets all expense summaries for the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `includeItemizations` | string | No | — | Include all itemizations of an Expense, if any |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "includeItemizations": {
      "type": "string",
      "description": "Include all itemizations of an Expense, if any",
      "enum": [
        "false",
        "true"
      ]
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_reports_to_approve

Gets the reports to approve.

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `sort` | string | No | — | The name of the field on which to sort |
| `order` | string | No | — | Sort order |
| `includeDelegateApprovals` | boolean | Yes | — | If true, reports pending approvals of employees you delegate for are returned as well. |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "sort": {
      "type": "string",
      "description": "The name of the field on which to sort",
      "enum": [
        "amountDueEmployee",
        "claimedAmount",
        "employeeFirstName",
        "employeeLastName",
        "name",
        "reportDate",
        "submitDate"
      ]
    },
    "order": {
      "type": "string",
      "description": "Sort order",
      "enum": [
        "asc",
        "desc"
      ]
    },
    "includeDelegateApprovals": {
      "type": "boolean",
      "description": "If true, reports pending approvals of employees you delegate for are returned as well."
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "MANAGER"
      ]
    }
  },
  "required": [
    "PCID",
    "includeDelegateApprovals",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_get_request_associations

Gets the requests associated with the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "MANAGER",
        "PROCESSOR",
        "PROXY"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_post_expense_attendees

Associates a set of attendees to a specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `expenseAttendeeList` | any[] | No | — | The list of attendees associated with the expense |
| `noShowAttendeeCount` | integer | No | — | The number of attendees that were planned but did not show up |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    },
    "expenseAttendeeList": {
      "type": "array",
      "description": "The list of attendees associated with the expense"
    },
    "noShowAttendeeCount": {
      "type": "integer",
      "description": "The number of attendees that were planned but did not show up"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_post_new_report

Creates a new report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `businessPurpose` | string | No | — | The text input for the business purpose by the user |
| `comment` | string | No | — | The expense report comment added by the user |
| `country` | string | No | — | The country name associated with the report (localized as per accept-language header) |
| `countryCode` | string | No | — | The location country ISO 3166-1 code (https://en.wikipedia.org/wiki/ISO_3166-1) |
| `countrySubDivisionCode` | string | No | — | The location country sub division ISO 3166-2 code (https://en.wikipedia.org/wiki/ISO_3166-2) |
| `customData` | any[] | No | — | This is a set of data that the custom fields contain for the resource based on the configuration. This set might be empty if no custom fields are configured |
| `endDate` | string | No | — | The end date (ISO-8601) of the report as used for trip-based reporting (https://www.iso.org/iso-8601-date-and-time-format.html). Format: YYYY-MM-DD |
| `name` | string | Yes | — | The expense report name input by the user |
| `policy` | string | No | — | The policy name to which the report adheres to |
| `policyId` | string | Yes | — | The unique identifier of the policy that applies to this report |
| `reportDate` | string | No | — | The date (ISO-8601) assigned to the report by the user (https://www.iso.org/iso-8601-date-and-time-format.html).  Format: YYYY-MM-DD |
| `startDate` | string | No | — | The start date (ISO-8601) of the report as used for trip-based reporting (https://www.iso.org/iso-8601-date-and-time-format.html). Format: YYYY-MM-DD |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    },
    "businessPurpose": {
      "type": "string",
      "description": "The text input for the business purpose by the user"
    },
    "comment": {
      "type": "string",
      "description": "The expense report comment added by the user"
    },
    "country": {
      "type": "string",
      "description": "The country name associated with the report (localized as per accept-language header)"
    },
    "countryCode": {
      "type": "string",
      "description": "The location country ISO 3166-1 code (https://en.wikipedia.org/wiki/ISO_3166-1)"
    },
    "countrySubDivisionCode": {
      "type": "string",
      "description": "The location country sub division ISO 3166-2 code (https://en.wikipedia.org/wiki/ISO_3166-2)"
    },
    "customData": {
      "type": "array",
      "description": "This is a set of data that the custom fields contain for the resource based on the configuration. This set might be empty if no custom fields are configured"
    },
    "endDate": {
      "type": "string",
      "description": "The end date (ISO-8601) of the report as used for trip-based reporting (https://www.iso.org/iso-8601-date-and-time-format.html). Format: YYYY-MM-DD"
    },
    "name": {
      "type": "string",
      "description": "The expense report name input by the user"
    },
    "policy": {
      "type": "string",
      "description": "The policy name to which the report adheres to"
    },
    "policyId": {
      "type": "string",
      "description": "The unique identifier of the policy that applies to this report"
    },
    "reportDate": {
      "type": "string",
      "description": "The date (ISO-8601) assigned to the report by the user (https://www.iso.org/iso-8601-date-and-time-format.html).  Format: YYYY-MM-DD"
    },
    "startDate": {
      "type": "string",
      "description": "The start date (ISO-8601) of the report as used for trip-based reporting (https://www.iso.org/iso-8601-date-and-time-format.html). Format: YYYY-MM-DD"
    }
  },
  "required": [
    "PCID",
    "userId",
    "contextType",
    "name",
    "policyId"
  ]
}
```

</Expandable>

---

## sap_concur_recall_report

Recalls the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `body` | object | No | — | Request body |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    },
    "body": {
      "type": "object",
      "description": "Request body"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_save_allocation_exception

Adds an exception on the specified allocation

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `allocationId` | string | Yes | — | The unique identifier of the expense allocation |
| `exceptionCode` | string | Yes | — | The code for the exception |
| `exceptionVisibility` | string | Yes | — | The visibility of the exception to be added. Possible values: 'ALL' - The exception is visible to the Traveler, Approver, and Processor, 'APPROVER_PROCESSOR' - The exception is visible only to the Approver and Processor, 'PROCESSOR' - The exception is visible only to the Processor. Default value: ALL |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "allocationId": {
      "type": "string",
      "description": "The unique identifier of the expense allocation"
    },
    "exceptionCode": {
      "type": "string",
      "description": "The code for the exception"
    },
    "exceptionVisibility": {
      "type": "string",
      "description": "The visibility of the exception to be added. Possible values: 'ALL' - The exception is visible to the Traveler, Approver, and Processor, 'APPROVER_PROCESSOR' - The exception is visible only to the Approver and Processor, 'PROCESSOR' - The exception is visible only to the Processor. Default value: ALL",
      "enum": [
        "ALL",
        "APPROVER_PROCESSOR",
        "PROCESSOR"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "allocationId",
    "exceptionCode",
    "exceptionVisibility"
  ]
}
```

</Expandable>

---

## sap_concur_save_report_entry_exception

Adds an exception on the specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `exceptionCode` | string | Yes | — | The code for the exception |
| `exceptionVisibility` | string | Yes | — | The visibility of the exception to be added. Possible values: 'ALL' - The exception is visible to the Traveler, Approver, and Processor, 'APPROVER_PROCESSOR' - The exception is visible only to the Approver and Processor, 'PROCESSOR' - The exception is visible only to the Processor. Default value: ALL |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "exceptionCode": {
      "type": "string",
      "description": "The code for the exception"
    },
    "exceptionVisibility": {
      "type": "string",
      "description": "The visibility of the exception to be added. Possible values: 'ALL' - The exception is visible to the Traveler, Approver, and Processor, 'APPROVER_PROCESSOR' - The exception is visible only to the Approver and Processor, 'PROCESSOR' - The exception is visible only to the Processor. Default value: ALL",
      "enum": [
        "ALL",
        "APPROVER_PROCESSOR",
        "PROCESSOR"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "exceptionCode",
    "exceptionVisibility"
  ]
}
```

</Expandable>

---

## sap_concur_save_report_exception

Adds an exception on the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `exceptionCode` | string | Yes | — | The code for the exception |
| `exceptionVisibility` | string | Yes | — | The visibility of the exception to be added. Possible values: 'ALL' - The exception is visible to the Traveler, Approver, and Processor, 'APPROVER_PROCESSOR' - The exception is visible only to the Approver and Processor, 'PROCESSOR' - The exception is visible only to the Processor. Default value: ALL |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "exceptionCode": {
      "type": "string",
      "description": "The code for the exception"
    },
    "exceptionVisibility": {
      "type": "string",
      "description": "The visibility of the exception to be added. Possible values: 'ALL' - The exception is visible to the Traveler, Approver, and Processor, 'APPROVER_PROCESSOR' - The exception is visible only to the Approver and Processor, 'PROCESSOR' - The exception is visible only to the Processor. Default value: ALL",
      "enum": [
        "ALL",
        "APPROVER_PROCESSOR",
        "PROCESSOR"
      ]
    }
  },
  "required": [
    "PCID",
    "reportId",
    "exceptionCode",
    "exceptionVisibility"
  ]
}
```

</Expandable>

---

## sap_concur_send_back_report

Sends back the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `comment` | string | Yes | — | This is the approver's remark on the sendback action |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "MANAGER"
      ]
    },
    "comment": {
      "type": "string",
      "description": "This is the approver's remark on the sendback action"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType",
    "comment"
  ]
}
```

</Expandable>

---

## sap_concur_send_back_report_as_system

Sends back the specified report using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `comment` | string | Yes | — | This is the approver's remark on the sendback action |
| `expectedWorkflowStepInstanceId` | string | No | — | The ID of the workflow step in which this report is expected to be in.This parameter can be used to verify that the report is still in the expected workflow step. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "comment": {
      "type": "string",
      "description": "This is the approver's remark on the sendback action"
    },
    "expectedWorkflowStepInstanceId": {
      "type": "string",
      "description": "The ID of the workflow step in which this report is expected to be in.This parameter can be used to verify that the report is still in the expected workflow step."
    }
  },
  "required": [
    "PCID",
    "reportId",
    "comment"
  ]
}
```

</Expandable>

---

## sap_concur_update_report

Updates the report header

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `businessPurpose` | string | No | — | The text input for the business purpose by the user |
| `comment` | string | No | — | The expense report comment added by the user |
| `country` | string | No | — | The country name associated with the report (localized as per accept-language header) |
| `countryCode` | string | No | — | The location country ISO 3166-1 code (https://en.wikipedia.org/wiki/ISO_3166-1) |
| `countrySubDivisionCode` | string | No | — | The location country sub division ISO 3166-2 code (https://en.wikipedia.org/wiki/ISO_3166-2) |
| `customData` | any[] | No | — | This is a set of data that the custom fields contain for the resource based on the configuration. This set might be empty if no custom fields are configured |
| `endDate` | string | No | — | The end date (ISO-8601) of the report as used for trip-based reporting (https://www.iso.org/iso-8601-date-and-time-format.html). Format: YYYY-MM-DD |
| `hasReceivedCashAdvanceReturns` | boolean | No | — | Whether the Processor has received funds from the user for a cash advance return |
| `isCopyDownInherited` | boolean | No | — | If this flag is set to true, any change in the report header fields will be copied down to expenses, itemizations and allocations, as per the configuration. Usage of this flag should be deliberate as data corruption could result. |
| `isPaperReceiptsReceived` | boolean | No | — | Whether paper receipts have been received for the report |
| `name` | string | No | — | The expense report name input by the user |
| `policy` | string | No | — | The policy name to which the report adheres to |
| `policyId` | string | No | — | The unique identifier of the policy that applies to this report |
| `receiptContainerId` | string | No | — | The unique identifier of the physical location where the paper receipts are stored |
| `redirectFund` | object | No | — | Payment redirected to card at user's request |
| `reportDate` | string | No | — | The date (ISO-8601) assigned to the report by the user (https://www.iso.org/iso-8601-date-and-time-format.html).  Format: YYYY-MM-DD |
| `startDate` | string | No | — | The start date (ISO-8601) of the report as used for trip-based reporting (https://www.iso.org/iso-8601-date-and-time-format.html). Format: YYYY-MM-DD |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    },
    "businessPurpose": {
      "type": "string",
      "description": "The text input for the business purpose by the user"
    },
    "comment": {
      "type": "string",
      "description": "The expense report comment added by the user"
    },
    "country": {
      "type": "string",
      "description": "The country name associated with the report (localized as per accept-language header)"
    },
    "countryCode": {
      "type": "string",
      "description": "The location country ISO 3166-1 code (https://en.wikipedia.org/wiki/ISO_3166-1)"
    },
    "countrySubDivisionCode": {
      "type": "string",
      "description": "The location country sub division ISO 3166-2 code (https://en.wikipedia.org/wiki/ISO_3166-2)"
    },
    "customData": {
      "type": "array",
      "description": "This is a set of data that the custom fields contain for the resource based on the configuration. This set might be empty if no custom fields are configured"
    },
    "endDate": {
      "type": "string",
      "description": "The end date (ISO-8601) of the report as used for trip-based reporting (https://www.iso.org/iso-8601-date-and-time-format.html). Format: YYYY-MM-DD"
    },
    "hasReceivedCashAdvanceReturns": {
      "type": "boolean",
      "description": "Whether the Processor has received funds from the user for a cash advance return"
    },
    "isCopyDownInherited": {
      "type": "boolean",
      "description": "If this flag is set to true, any change in the report header fields will be copied down to expenses, itemizations and allocations, as per the configuration. Usage of this flag should be deliberate as data corruption could result."
    },
    "isPaperReceiptsReceived": {
      "type": "boolean",
      "description": "Whether paper receipts have been received for the report"
    },
    "name": {
      "type": "string",
      "description": "The expense report name input by the user"
    },
    "policy": {
      "type": "string",
      "description": "The policy name to which the report adheres to"
    },
    "policyId": {
      "type": "string",
      "description": "The unique identifier of the policy that applies to this report"
    },
    "receiptContainerId": {
      "type": "string",
      "description": "The unique identifier of the physical location where the paper receipts are stored"
    },
    "redirectFund": {
      "type": "object",
      "description": "Payment redirected to card at user's request",
      "properties": {
        "creditCardId": {
          "type": "string",
          "description": "The unique identifier of the IBCP card account to which funds need to be redirected"
        },
        "amount": {
          "type": "object",
          "description": "The amount comprises of value and currency."
        }
      },
      "required": [
        "amount",
        "creditCardId"
      ]
    },
    "reportDate": {
      "type": "string",
      "description": "The date (ISO-8601) assigned to the report by the user (https://www.iso.org/iso-8601-date-and-time-format.html).  Format: YYYY-MM-DD"
    },
    "startDate": {
      "type": "string",
      "description": "The start date (ISO-8601) of the report as used for trip-based reporting (https://www.iso.org/iso-8601-date-and-time-format.html). Format: YYYY-MM-DD"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType"
  ]
}
```

</Expandable>

---

## sap_concur_update_report_allocation

Updates a specified allocation

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report that contains the expense associated with the allocations |
| `allocationId` | string | Yes | — | The unique identifier of the expense allocation |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `allocation` | object | Yes | — | Allocation data |
| `expenseIds` | any[] | Yes | — | This is an array of unique identifiers of expenses within this report that are being updated |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report that contains the expense associated with the allocations"
    },
    "allocationId": {
      "type": "string",
      "description": "The unique identifier of the expense allocation"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    },
    "allocation": {
      "type": "object",
      "description": "Allocation data",
      "properties": {
        "customData": {
          "type": "array",
          "description": "This is a set of data that the custom fields contain for the resource based on the configuration. This set might be empty if no custom fields are configured"
        }
      }
    },
    "expenseIds": {
      "type": "array",
      "description": "This is an array of unique identifiers of expenses within this report that are being updated"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "allocationId",
    "userId",
    "contextType",
    "allocation",
    "expenseIds"
  ]
}
```

</Expandable>

---

## sap_concur_update_report_comments

Updates a comment for the specified report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `comment` | string | Yes | — | The comments input on the report/expense by all users |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    },
    "comment": {
      "type": "string",
      "description": "The comments input on the report/expense by all users"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "userId",
    "contextType",
    "comment"
  ]
}
```

</Expandable>

---

## sap_concur_update_report_comments_with_company_jwt

Updates a comment for the specified report using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `comment` | string | Yes | — | The comments input on the report/expense by all users |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "comment": {
      "type": "string",
      "description": "The comments input on the report/expense by all users"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "comment"
  ]
}
```

</Expandable>

---

## sap_concur_update_report_entry_comments

Updates a comment for the specified expense

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `userId` | string | Yes | — | The unique identifier of the Concur user |
| `contextType` | string | Yes | — | The access level of the Concur user, which determines the form fields they can view/modify |
| `comment` | string | Yes | — | The comments input on the report/expense by all users |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the Concur user"
    },
    "contextType": {
      "type": "string",
      "description": "The access level of the Concur user, which determines the form fields they can view/modify",
      "enum": [
        "TRAVELER",
        "PROXY"
      ]
    },
    "comment": {
      "type": "string",
      "description": "The comments input on the report/expense by all users"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "userId",
    "contextType",
    "comment"
  ]
}
```

</Expandable>

---

## sap_concur_update_report_entry_comments_with_company_jwt

Updates a comment for the specified expense using system user

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `comment` | string | Yes | — | The comments input on the report/expense by all users |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "comment": {
      "type": "string",
      "description": "The comments input on the report/expense by all users"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "comment"
  ]
}
```

</Expandable>

---

## sap_concur_update_report_expense_using_short_url

Updates (limited fields) the specified expense in an un-submitted or submitted report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `reportId` | string | Yes | — | The unique identifier of the report |
| `expenseId` | string | Yes | — | The unique identifier of the expense |
| `businessPurpose` | string | No | — | The text input for the business purpose by the user. |
| `customData` | any[] | No | — | This is a set of data that the custom fields contain for the resource based on the configuration. This set might be empty if no custom fields are configured |
| `expenseSource` | string | Yes | — | The source of the expense. Possible values: EA - Expense Assistant, MOB - Mobile, OTHER - Unknown, SE - Smart Expense, TA - Travel Allowance, TR - Travel Request, UI - Web UI |
| `isExpenseRejected` | boolean | No | — | Whether the approver or processor has rejected this expense in the report. If this is true then this expense will be sent back to the report submitted in an addendum (split) report |
| `isPaperReceiptReceived` | boolean | No | — | Whether paper receipts have been received for the expense |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "reportId": {
      "type": "string",
      "description": "The unique identifier of the report"
    },
    "expenseId": {
      "type": "string",
      "description": "The unique identifier of the expense"
    },
    "businessPurpose": {
      "type": "string",
      "description": "The text input for the business purpose by the user."
    },
    "customData": {
      "type": "array",
      "description": "This is a set of data that the custom fields contain for the resource based on the configuration. This set might be empty if no custom fields are configured"
    },
    "expenseSource": {
      "type": "string",
      "description": "The source of the expense. Possible values: EA - Expense Assistant, MOB - Mobile, OTHER - Unknown, SE - Smart Expense, TA - Travel Allowance, TR - Travel Request, UI - Web UI",
      "enum": [
        "EA",
        "MOB",
        "OTHER",
        "SE",
        "TA",
        "TR",
        "UI"
      ]
    },
    "isExpenseRejected": {
      "type": "boolean",
      "description": "Whether the approver or processor has rejected this expense in the report. If this is true then this expense will be sent back to the report submitted in an addendum (split) report"
    },
    "isPaperReceiptReceived": {
      "type": "boolean",
      "description": "Whether paper receipts have been received for the expense"
    }
  },
  "required": [
    "PCID",
    "reportId",
    "expenseId",
    "expenseSource"
  ]
}
```

</Expandable>
