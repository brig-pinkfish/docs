---
title: "talkdesk"
sidebarTitle: "talkdesk"
description: "Contact center agents, calls, queues, and recordings"
---

{/* Auto-generated by scripts/generate-application-mcp-docs.ts — do not edit manually */}

**Server path:** `/talkdesk` | **Type:** Application | **PCID required:** Yes

Contact center agents, calls, queues, and recordings

## Tools

| Tool | Description |
| --- | --- |
| [`talkdesk_get_agents`](#talkdesk_get_agents) | Get list of agents in the contact center |
| [`talkdesk_get_agent`](#talkdesk_get_agent) | Get detailed information about a specific agent |
| [`talkdesk_get_calls`](#talkdesk_get_calls) | Get call records with optional filtering |
| [`talkdesk_get_call`](#talkdesk_get_call) | Get detailed information about a specific call |
| [`talkdesk_create_contact`](#talkdesk_create_contact) | Create a new contact in Talkdesk |
| [`talkdesk_get_contact`](#talkdesk_get_contact) | Retrieve a contact by ID or phone number |
| [`talkdesk_update_contact`](#talkdesk_update_contact) | Update an existing contact |
| [`talkdesk_get_queues`](#talkdesk_get_queues) | Get list of call queues |
| [`talkdesk_get_queue_stats`](#talkdesk_get_queue_stats) | Get statistics for a specific queue |
| [`talkdesk_get_agent_stats`](#talkdesk_get_agent_stats) | Get performance statistics for agents |
| [`talkdesk_create_callback`](#talkdesk_create_callback) | Schedule a callback for a customer |
| [`talkdesk_get_recordings`](#talkdesk_get_recordings) | Get call recordings with optional filtering |

---

## talkdesk_get_agents

Get list of agents in the contact center

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `status` | string | No | — | Filter agents by status |
| `limit` | number | No | `50` | Maximum number of agents to return (1-100) |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "status": {
      "type": "string",
      "enum": [
        "online",
        "offline",
        "busy",
        "away"
      ],
      "description": "Filter agents by status"
    },
    "limit": {
      "type": "number",
      "default": 50,
      "description": "Maximum number of agents to return (1-100)"
    }
  },
  "required": [
    "PCID"
  ]
}
```

</Expandable>

---

## talkdesk_get_agent

Get detailed information about a specific agent

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `agentId` | string | Yes | — | Agent ID to retrieve |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "agentId": {
      "type": "string",
      "description": "Agent ID to retrieve"
    }
  },
  "required": [
    "PCID",
    "agentId"
  ]
}
```

</Expandable>

---

## talkdesk_get_calls

Get call records with optional filtering

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `startDate` | string | Yes | — | Start date for call records (YYYY-MM-DD format) |
| `endDate` | string | Yes | — | End date for call records (YYYY-MM-DD format) |
| `agentId` | string | No | — | Filter by specific agent ID |
| `direction` | string | No | — | Filter by call direction |
| `status` | string | No | — | Filter by call status |
| `limit` | number | No | `100` | Maximum number of calls to return (1-1000) |
| `offset` | number | No | `0` | Offset for pagination |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "startDate": {
      "type": "string",
      "description": "Start date for call records (YYYY-MM-DD format)"
    },
    "endDate": {
      "type": "string",
      "description": "End date for call records (YYYY-MM-DD format)"
    },
    "agentId": {
      "type": "string",
      "description": "Filter by specific agent ID"
    },
    "direction": {
      "type": "string",
      "enum": [
        "inbound",
        "outbound"
      ],
      "description": "Filter by call direction"
    },
    "status": {
      "type": "string",
      "enum": [
        "answered",
        "missed",
        "abandoned"
      ],
      "description": "Filter by call status"
    },
    "limit": {
      "type": "number",
      "default": 100,
      "description": "Maximum number of calls to return (1-1000)"
    },
    "offset": {
      "type": "number",
      "default": 0,
      "description": "Offset for pagination"
    }
  },
  "required": [
    "PCID",
    "startDate",
    "endDate"
  ]
}
```

</Expandable>

---

## talkdesk_get_call

Get detailed information about a specific call

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `callId` | string | Yes | — | Call ID to retrieve |
| `includeRecording` | boolean | No | `false` | Include call recording URL if available |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "callId": {
      "type": "string",
      "description": "Call ID to retrieve"
    },
    "includeRecording": {
      "type": "boolean",
      "default": false,
      "description": "Include call recording URL if available"
    }
  },
  "required": [
    "PCID",
    "callId"
  ]
}
```

</Expandable>

---

## talkdesk_create_contact

Create a new contact in Talkdesk

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `name` | string | Yes | — | Contact name |
| `phoneNumber` | string | Yes | — | Contact phone number |
| `email` | string | No | — | Contact email address |
| `company` | string | No | — | Contact company |
| `notes` | string | No | — | Contact notes |
| `tags` | string[] | No | — | Tags to associate with the contact |
| `customFields` | object | No | — | Custom field values |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "name": {
      "type": "string",
      "description": "Contact name"
    },
    "phoneNumber": {
      "type": "string",
      "description": "Contact phone number"
    },
    "email": {
      "type": "string",
      "description": "Contact email address"
    },
    "company": {
      "type": "string",
      "description": "Contact company"
    },
    "notes": {
      "type": "string",
      "description": "Contact notes"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Tags to associate with the contact"
    },
    "customFields": {
      "type": "object",
      "additionalProperties": true,
      "description": "Custom field values"
    }
  },
  "required": [
    "PCID",
    "name",
    "phoneNumber"
  ]
}
```

</Expandable>

---

## talkdesk_get_contact

Retrieve a contact by ID or phone number

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `contactId` | string | No | — | Contact ID |
| `phoneNumber` | string | No | — | Contact phone number |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "contactId": {
      "type": "string",
      "description": "Contact ID"
    },
    "phoneNumber": {
      "type": "string",
      "description": "Contact phone number"
    }
  },
  "required": [
    "PCID"
  ]
}
```

</Expandable>

---

## talkdesk_update_contact

Update an existing contact

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `contactId` | string | Yes | — | Contact ID to update |
| `name` | string | No | — | Updated contact name |
| `phoneNumber` | string | No | — | Updated phone number |
| `email` | string | No | — | Updated email address |
| `company` | string | No | — | Updated company |
| `notes` | string | No | — | Updated notes |
| `tags` | string[] | No | — | Updated tags |
| `customFields` | object | No | — | Updated custom field values |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "contactId": {
      "type": "string",
      "description": "Contact ID to update"
    },
    "name": {
      "type": "string",
      "description": "Updated contact name"
    },
    "phoneNumber": {
      "type": "string",
      "description": "Updated phone number"
    },
    "email": {
      "type": "string",
      "description": "Updated email address"
    },
    "company": {
      "type": "string",
      "description": "Updated company"
    },
    "notes": {
      "type": "string",
      "description": "Updated notes"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Updated tags"
    },
    "customFields": {
      "type": "object",
      "additionalProperties": true,
      "description": "Updated custom field values"
    }
  },
  "required": [
    "PCID",
    "contactId"
  ]
}
```

</Expandable>

---

## talkdesk_get_queues

Get list of call queues

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    }
  },
  "required": [
    "PCID"
  ]
}
```

</Expandable>

---

## talkdesk_get_queue_stats

Get statistics for a specific queue

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `queueId` | string | Yes | — | Queue ID |
| `startDate` | string | Yes | — | Start date for statistics (YYYY-MM-DD format) |
| `endDate` | string | Yes | — | End date for statistics (YYYY-MM-DD format) |
| `interval` | string | No | `"day"` | Time interval for aggregation |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "queueId": {
      "type": "string",
      "description": "Queue ID"
    },
    "startDate": {
      "type": "string",
      "description": "Start date for statistics (YYYY-MM-DD format)"
    },
    "endDate": {
      "type": "string",
      "description": "End date for statistics (YYYY-MM-DD format)"
    },
    "interval": {
      "type": "string",
      "enum": [
        "hour",
        "day",
        "week",
        "month"
      ],
      "default": "day",
      "description": "Time interval for aggregation"
    }
  },
  "required": [
    "PCID",
    "queueId",
    "startDate",
    "endDate"
  ]
}
```

</Expandable>

---

## talkdesk_get_agent_stats

Get performance statistics for agents

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `agentId` | string | No | — | Specific agent ID (if not provided, gets all agents) |
| `startDate` | string | Yes | — | Start date for statistics (YYYY-MM-DD format) |
| `endDate` | string | Yes | — | End date for statistics (YYYY-MM-DD format) |
| `metrics` | string[] | No | — | Specific metrics to retrieve |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "agentId": {
      "type": "string",
      "description": "Specific agent ID (if not provided, gets all agents)"
    },
    "startDate": {
      "type": "string",
      "description": "Start date for statistics (YYYY-MM-DD format)"
    },
    "endDate": {
      "type": "string",
      "description": "End date for statistics (YYYY-MM-DD format)"
    },
    "metrics": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "calls_answered",
          "calls_missed",
          "average_talk_time",
          "average_wait_time",
          "total_talk_time"
        ]
      },
      "description": "Specific metrics to retrieve"
    }
  },
  "required": [
    "PCID",
    "startDate",
    "endDate"
  ]
}
```

</Expandable>

---

## talkdesk_create_callback

Schedule a callback for a customer

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `phoneNumber` | string | Yes | — | Customer phone number for callback |
| `scheduledTime` | string | Yes | — | Scheduled callback time (YYYY-MM-DD HH:MM:SS) |
| `agentId` | string | No | — | Specific agent ID to assign callback |
| `queueId` | string | No | — | Queue ID to route callback to |
| `notes` | string | No | — | Callback notes |
| `priority` | string | No | `"normal"` | Callback priority |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "phoneNumber": {
      "type": "string",
      "description": "Customer phone number for callback"
    },
    "scheduledTime": {
      "type": "string",
      "description": "Scheduled callback time (YYYY-MM-DD HH:MM:SS)"
    },
    "agentId": {
      "type": "string",
      "description": "Specific agent ID to assign callback"
    },
    "queueId": {
      "type": "string",
      "description": "Queue ID to route callback to"
    },
    "notes": {
      "type": "string",
      "description": "Callback notes"
    },
    "priority": {
      "type": "string",
      "enum": [
        "low",
        "normal",
        "high",
        "urgent"
      ],
      "default": "normal",
      "description": "Callback priority"
    }
  },
  "required": [
    "PCID",
    "phoneNumber",
    "scheduledTime"
  ]
}
```

</Expandable>

---

## talkdesk_get_recordings

Get call recordings with optional filtering

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `startDate` | string | Yes | — | Start date for recordings (YYYY-MM-DD format) |
| `endDate` | string | Yes | — | End date for recordings (YYYY-MM-DD format) |
| `agentId` | string | No | — | Filter by specific agent ID |
| `callId` | string | No | — | Filter by specific call ID |
| `limit` | number | No | `50` | Maximum number of recordings to return (1-100) |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "startDate": {
      "type": "string",
      "description": "Start date for recordings (YYYY-MM-DD format)"
    },
    "endDate": {
      "type": "string",
      "description": "End date for recordings (YYYY-MM-DD format)"
    },
    "agentId": {
      "type": "string",
      "description": "Filter by specific agent ID"
    },
    "callId": {
      "type": "string",
      "description": "Filter by specific call ID"
    },
    "limit": {
      "type": "number",
      "default": 50,
      "description": "Maximum number of recordings to return (1-100)"
    }
  },
  "required": [
    "PCID",
    "startDate",
    "endDate"
  ]
}
```

</Expandable>
