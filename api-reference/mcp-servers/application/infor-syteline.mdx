---
title: "infor-syteline"
sidebarTitle: "infor-syteline"
description: "SyteLine ERP"
---

{/* Auto-generated by scripts/generate-application-mcp-docs.ts — do not edit manually. To regenerate: see docs/scripts/README.md */}

**Server path:** `/infor-syteline` | **Type:** Application | **PCID required:** Yes

## Tools

| Tool | Description |
| --- | --- |
| [`infor_syteline_get_idoproperty_metadata`](#infor_syteline_get_idoproperty_metadata) | Get property metadata for an IDO |
| [`infor_syteline_invoke_idomethod`](#infor_syteline_invoke_idomethod) | Invoke a custom IDO method |
| [`infor_syteline_load_ido`](#infor_syteline_load_ido) | Load (query) records from an IDO |
| [`infor_syteline_update_ido`](#infor_syteline_update_ido) | Create, update, or delete records in an IDO |

---

## infor_syteline_get_idoproperty_metadata

Get property metadata for an IDO

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `idoName` | string | Yes | — | The name of the IDO to get metadata for (e.g., SLItems, SLCustomers, SLCos, SLPos, UserNames, SLParts) |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "idoName": {
      "type": "string",
      "description": "The name of the IDO to get metadata for (e.g., SLItems, SLCustomers, SLCos, SLPos, UserNames, SLParts)"
    }
  },
  "required": [
    "PCID",
    "idoName"
  ]
}
```

</Expandable>

---

## infor_syteline_invoke_idomethod

Invoke a custom IDO method

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `methodName` | string | Yes | — | The name of the IDO method to invoke. Methods are defined on specific IDOs and perform custom business logic. |
| `body` | object | No | — | Method parameters as key-value pairs. The required parameters depend on the specific method being invoked. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "methodName": {
      "type": "string",
      "description": "The name of the IDO method to invoke. Methods are defined on specific IDOs and perform custom business logic."
    },
    "body": {
      "type": "object",
      "description": "Method parameters as key-value pairs. The required parameters depend on the specific method being invoked."
    }
  },
  "required": [
    "PCID",
    "methodName"
  ]
}
```

</Expandable>

---

## infor_syteline_load_ido

Load (query) records from an IDO

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `idoName` | string | Yes | — | The name of the IDO to query (e.g., SLItems, SLCustomers, SLCos, SLPos, UserNames, SLParts). Use the getIDOPropertyMetadata endpoint to discover available IDOs and their properties. |
| `properties` | string | No | — | Comma-separated list of property (field) names to return. If omitted, all properties are returned. Example: 'Item,Description,UM,UnitCost' |
| `filter` | string | No | — | SQL WHERE clause expression to filter results. Example: "Item LIKE 'ABC%'" or "CustNum = '1001'" or "Status = 'O' AND OrderDate &gt;= '2024-01-01'" |
| `orderby` | string | No | — | Comma-separated list of properties to sort by. Append ' DESC' for descending order. Example: 'OrderDate DESC,CustNum' |
| `RecordCap` | integer | No | — | Maximum number of records to return. Use for pagination or to limit large result sets. Default varies by server configuration. |
| `Bookmark` | string | No | — | Pagination cursor returned from a previous request. Pass this value to retrieve the next page of results. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "idoName": {
      "type": "string",
      "description": "The name of the IDO to query (e.g., SLItems, SLCustomers, SLCos, SLPos, UserNames, SLParts). Use the getIDOPropertyMetadata endpoint to discover available IDOs and their properties."
    },
    "properties": {
      "type": "string",
      "description": "Comma-separated list of property (field) names to return. If omitted, all properties are returned. Example: 'Item,Description,UM,UnitCost'"
    },
    "filter": {
      "type": "string",
      "description": "SQL WHERE clause expression to filter results. Example: \"Item LIKE 'ABC%'\" or \"CustNum = '1001'\" or \"Status = 'O' AND OrderDate >= '2024-01-01'\""
    },
    "orderby": {
      "type": "string",
      "description": "Comma-separated list of properties to sort by. Append ' DESC' for descending order. Example: 'OrderDate DESC,CustNum'"
    },
    "RecordCap": {
      "type": "integer",
      "description": "Maximum number of records to return. Use for pagination or to limit large result sets. Default varies by server configuration."
    },
    "Bookmark": {
      "type": "string",
      "description": "Pagination cursor returned from a previous request. Pass this value to retrieve the next page of results."
    }
  },
  "required": [
    "PCID",
    "idoName"
  ]
}
```

</Expandable>

---

## infor_syteline_update_ido

Create, update, or delete records in an IDO

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `idoName` | string | Yes | — | The name of the IDO to modify (e.g., SLItems, SLCustomers, SLCos). Use the getIDOPropertyMetadata endpoint to discover available properties and their types. |
| `Changes` | object[] | No | — | Array of record changes to apply. Each item must include '_Action' ('I' for insert, 'U' for update, 'D' for delete) and the relevant property values. |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "idoName": {
      "type": "string",
      "description": "The name of the IDO to modify (e.g., SLItems, SLCustomers, SLCos). Use the getIDOPropertyMetadata endpoint to discover available properties and their types."
    },
    "Changes": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "description": "Array of record changes to apply. Each item must include '_Action' ('I' for insert, 'U' for update, 'D' for delete) and the relevant property values."
    }
  },
  "required": [
    "PCID",
    "idoName"
  ]
}
```

</Expandable>
