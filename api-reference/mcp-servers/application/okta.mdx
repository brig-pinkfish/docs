---
title: "okta"
description: "Identity and access management"
---

{/* Auto-generated by scripts/generate-application-mcp-docs.ts — do not edit manually */}

**Server path:** `/okta` | **Type:** Application | **PCID required:** Yes

Identity and access management

## Tools

| Tool | Description |
| --- | --- |
| [`okta_list_users`](#okta_list_users) | List users in Okta |
| [`okta_get_user`](#okta_get_user) | Get a specific user by ID or login |
| [`okta_create_user`](#okta_create_user) | Create a new user in Okta |
| [`okta_update_user`](#okta_update_user) | Update an existing user (partial update - only specified fields are updated) |
| [`okta_deactivate_user`](#okta_deactivate_user) | Deactivate a user account |
| [`okta_list_groups`](#okta_list_groups) | List groups in Okta |
| [`okta_get_group`](#okta_get_group) | Get a specific group by ID |
| [`okta_add_user_to_group`](#okta_add_user_to_group) | Add a user to a group |
| [`okta_remove_user_from_group`](#okta_remove_user_from_group) | Remove a user from a group |
| [`okta_create_group`](#okta_create_group) | Create a new group in Okta |
| [`okta_update_group`](#okta_update_group) | Update an existing group |
| [`okta_delete_group`](#okta_delete_group) | Delete a group from Okta |
| [`okta_list_group_members`](#okta_list_group_members) | List all members (users) assigned to a group |
| [`okta_list_group_applications`](#okta_list_group_applications) | List all applications assigned to a group |
| [`okta_get_system_logs`](#okta_get_system_logs) | Retrieve Okta system logs for auditing and monitoring |

---

## okta_list_users

List users in Okta

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `q` | string | No | — | Search query for users |
| `filter` | string | No | — | Filter expression |
| `limit` | number | No | `200` | Maximum number of users to return |
| `after` | string | No | — | Cursor for pagination |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "q": {
      "type": "string",
      "description": "Search query for users"
    },
    "filter": {
      "type": "string",
      "description": "Filter expression"
    },
    "limit": {
      "type": "number",
      "default": 200,
      "description": "Maximum number of users to return"
    },
    "after": {
      "type": "string",
      "description": "Cursor for pagination"
    }
  },
  "required": [
    "PCID"
  ]
}
```
</Expandable>

---

## okta_get_user

Get a specific user by ID or login

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `userId` | string | Yes | — | User ID or login to retrieve |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "userId": {
      "type": "string",
      "description": "User ID or login to retrieve"
    }
  },
  "required": [
    "PCID",
    "userId"
  ]
}
```
</Expandable>

---

## okta_create_user

Create a new user in Okta

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `profile` | object | Yes | — | User profile information |
| `credentials` | object | No | — | User credentials (password and/or recovery question) |
| `groupIds` | string[] | No | — | Array of group IDs to add user to (max 20) |
| `activate` | boolean | No | `true` | Execute activation lifecycle operation when creating the user (default true) |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "profile": {
      "type": "object",
      "properties": {
        "firstName": {
          "type": "string",
          "description": "User first name"
        },
        "lastName": {
          "type": "string",
          "description": "User last name"
        },
        "email": {
          "type": "string",
          "description": "User email address"
        },
        "login": {
          "type": "string",
          "description": "User login name"
        },
        "mobilePhone": {
          "type": "string",
          "description": "Mobile phone number"
        },
        "secondEmail": {
          "type": "string",
          "description": "Secondary email"
        },
        "nickName": {
          "type": "string",
          "description": "Casual way to address the user"
        },
        "displayName": {
          "type": "string",
          "description": "Display name"
        },
        "profileUrl": {
          "type": "string",
          "description": "URL of user profile"
        },
        "preferredLanguage": {
          "type": "string",
          "description": "User preferred written or spoken language"
        },
        "userType": {
          "type": "string",
          "description": "Organization or application user type"
        },
        "organization": {
          "type": "string",
          "description": "Organization name"
        },
        "title": {
          "type": "string",
          "description": "User title"
        },
        "division": {
          "type": "string",
          "description": "Division"
        },
        "department": {
          "type": "string",
          "description": "Department"
        },
        "costCenter": {
          "type": "string",
          "description": "Cost center"
        },
        "employeeNumber": {
          "type": "string",
          "description": "Employee number"
        },
        "primaryPhone": {
          "type": "string",
          "description": "Primary phone number"
        },
        "streetAddress": {
          "type": "string",
          "description": "Street address"
        },
        "city": {
          "type": "string",
          "description": "City"
        },
        "state": {
          "type": "string",
          "description": "State"
        },
        "zipCode": {
          "type": "string",
          "description": "ZIP code"
        },
        "countryCode": {
          "type": "string",
          "description": "Country code"
        }
      },
      "description": "User profile information"
    },
    "credentials": {
      "type": "object",
      "properties": {
        "password": {
          "type": "object",
          "properties": {
            "value": {
              "type": "string",
              "description": "Password value"
            }
          },
          "description": "Password credentials"
        },
        "recovery_question": {
          "type": "object",
          "properties": {
            "question": {
              "type": "string",
              "description": "Recovery question"
            },
            "answer": {
              "type": "string",
              "description": "Recovery answer"
            }
          },
          "description": "Recovery question and answer"
        }
      },
      "description": "User credentials (password and/or recovery question)"
    },
    "groupIds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of group IDs to add user to (max 20)"
    },
    "activate": {
      "type": "boolean",
      "default": true,
      "description": "Execute activation lifecycle operation when creating the user (default true)"
    }
  },
  "required": [
    "PCID",
    "profile"
  ]
}
```
</Expandable>

---

## okta_update_user

Update an existing user (partial update - only specified fields are updated)

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `userId` | string | Yes | — | User ID to update |
| `profile` | object | No | — | Updated profile information |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "userId": {
      "type": "string",
      "description": "User ID to update"
    },
    "profile": {
      "type": "object",
      "properties": {
        "firstName": {
          "type": "string",
          "description": "Updated first name"
        },
        "lastName": {
          "type": "string",
          "description": "Updated last name"
        },
        "email": {
          "type": "string",
          "description": "Updated email address"
        },
        "login": {
          "type": "string",
          "description": "Updated login name"
        },
        "mobilePhone": {
          "type": "string",
          "description": "Updated mobile phone"
        },
        "secondEmail": {
          "type": "string",
          "description": "Updated secondary email"
        },
        "nickName": {
          "type": "string",
          "description": "Updated nickname"
        },
        "displayName": {
          "type": "string",
          "description": "Updated display name"
        },
        "profileUrl": {
          "type": "string",
          "description": "Updated profile URL"
        },
        "preferredLanguage": {
          "type": "string",
          "description": "Updated preferred language"
        },
        "userType": {
          "type": "string",
          "description": "Updated user type"
        },
        "organization": {
          "type": "string",
          "description": "Updated organization"
        },
        "title": {
          "type": "string",
          "description": "Updated title"
        },
        "division": {
          "type": "string",
          "description": "Updated division"
        },
        "department": {
          "type": "string",
          "description": "Updated department"
        },
        "costCenter": {
          "type": "string",
          "description": "Updated cost center"
        },
        "employeeNumber": {
          "type": "string",
          "description": "Updated employee number"
        },
        "primaryPhone": {
          "type": "string",
          "description": "Updated primary phone"
        },
        "streetAddress": {
          "type": "string",
          "description": "Updated street address"
        },
        "city": {
          "type": "string",
          "description": "Updated city"
        },
        "state": {
          "type": "string",
          "description": "Updated state"
        },
        "zipCode": {
          "type": "string",
          "description": "Updated ZIP code"
        },
        "countryCode": {
          "type": "string",
          "description": "Updated country code"
        }
      },
      "description": "Updated profile information"
    }
  },
  "required": [
    "PCID",
    "userId"
  ]
}
```
</Expandable>

---

## okta_deactivate_user

Deactivate a user account

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `userId` | string | Yes | — | User ID to deactivate |
| `sendEmail` | boolean | No | `false` | Send deactivation email to user |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "userId": {
      "type": "string",
      "description": "User ID to deactivate"
    },
    "sendEmail": {
      "type": "boolean",
      "default": false,
      "description": "Send deactivation email to user"
    }
  },
  "required": [
    "PCID",
    "userId"
  ]
}
```
</Expandable>

---

## okta_list_groups

List groups in Okta

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `q` | string | No | — | Search query for groups |
| `filter` | string | No | — | Filter expression |
| `limit` | number | No | `200` | Maximum number of groups to return |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "q": {
      "type": "string",
      "description": "Search query for groups"
    },
    "filter": {
      "type": "string",
      "description": "Filter expression"
    },
    "limit": {
      "type": "number",
      "default": 200,
      "description": "Maximum number of groups to return"
    }
  },
  "required": [
    "PCID"
  ]
}
```
</Expandable>

---

## okta_get_group

Get a specific group by ID

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `groupId` | string | Yes | — | Group ID to retrieve |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "groupId": {
      "type": "string",
      "description": "Group ID to retrieve"
    }
  },
  "required": [
    "PCID",
    "groupId"
  ]
}
```
</Expandable>

---

## okta_add_user_to_group

Add a user to a group

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `groupId` | string | Yes | — | Group ID |
| `userId` | string | Yes | — | User ID to add to group |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "groupId": {
      "type": "string",
      "description": "Group ID"
    },
    "userId": {
      "type": "string",
      "description": "User ID to add to group"
    }
  },
  "required": [
    "PCID",
    "groupId",
    "userId"
  ]
}
```
</Expandable>

---

## okta_remove_user_from_group

Remove a user from a group

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `groupId` | string | Yes | — | Group ID |
| `userId` | string | Yes | — | User ID to remove from group |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "groupId": {
      "type": "string",
      "description": "Group ID"
    },
    "userId": {
      "type": "string",
      "description": "User ID to remove from group"
    }
  },
  "required": [
    "PCID",
    "groupId",
    "userId"
  ]
}
```
</Expandable>

---

## okta_create_group

Create a new group in Okta

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `profile` | object | Yes | — | Group profile information |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "profile": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Group name"
        },
        "description": {
          "type": "string",
          "description": "Group description"
        }
      },
      "description": "Group profile information"
    }
  },
  "required": [
    "PCID",
    "profile"
  ]
}
```
</Expandable>

---

## okta_update_group

Update an existing group

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `groupId` | string | Yes | — | Group ID to update |
| `profile` | object | Yes | — | Updated group profile information |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "groupId": {
      "type": "string",
      "description": "Group ID to update"
    },
    "profile": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Updated group name"
        },
        "description": {
          "type": "string",
          "description": "Updated group description"
        }
      },
      "description": "Updated group profile information"
    }
  },
  "required": [
    "PCID",
    "groupId",
    "profile"
  ]
}
```
</Expandable>

---

## okta_delete_group

Delete a group from Okta

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `groupId` | string | Yes | — | Group ID to delete |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "groupId": {
      "type": "string",
      "description": "Group ID to delete"
    }
  },
  "required": [
    "PCID",
    "groupId"
  ]
}
```
</Expandable>

---

## okta_list_group_members

List all members (users) assigned to a group

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `groupId` | string | Yes | — | Group ID to list members for |
| `limit` | number | No | `200` | Maximum number of members to return |
| `after` | string | No | — | Cursor for pagination |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "groupId": {
      "type": "string",
      "description": "Group ID to list members for"
    },
    "limit": {
      "type": "number",
      "default": 200,
      "description": "Maximum number of members to return"
    },
    "after": {
      "type": "string",
      "description": "Cursor for pagination"
    }
  },
  "required": [
    "PCID",
    "groupId"
  ]
}
```
</Expandable>

---

## okta_list_group_applications

List all applications assigned to a group

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `groupId` | string | Yes | — | Group ID to list applications for |
| `limit` | number | No | `20` | Maximum number of applications to return |
| `after` | string | No | — | Cursor for pagination |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "groupId": {
      "type": "string",
      "description": "Group ID to list applications for"
    },
    "limit": {
      "type": "number",
      "default": 20,
      "description": "Maximum number of applications to return"
    },
    "after": {
      "type": "string",
      "description": "Cursor for pagination"
    }
  },
  "required": [
    "PCID",
    "groupId"
  ]
}
```
</Expandable>

---

## okta_get_system_logs

Retrieve Okta system logs for auditing and monitoring

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `since` | string | No | — | Timestamp to start from (ISO 8601 format) |
| `until` | string | No | — | Timestamp to end at (ISO 8601 format) |
| `filter` | string | No | — | Filter expression (e.g., "eventType eq \"user.session.start\"") |
| `q` | string | No | — | Search query for log events |
| `limit` | number | No | `100` | Maximum number of log entries to return |
| `sortOrder` | string | No | `"DESCENDING"` | Sort order for log entries |
| `after` | string | No | — | Cursor for pagination |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "since": {
      "type": "string",
      "description": "Timestamp to start from (ISO 8601 format)"
    },
    "until": {
      "type": "string",
      "description": "Timestamp to end at (ISO 8601 format)"
    },
    "filter": {
      "type": "string",
      "description": "Filter expression (e.g., \"eventType eq \\\"user.session.start\\\"\")"
    },
    "q": {
      "type": "string",
      "description": "Search query for log events"
    },
    "limit": {
      "type": "number",
      "default": 100,
      "description": "Maximum number of log entries to return"
    },
    "sortOrder": {
      "type": "string",
      "enum": [
        "ASCENDING",
        "DESCENDING"
      ],
      "default": "DESCENDING",
      "description": "Sort order for log entries"
    },
    "after": {
      "type": "string",
      "description": "Cursor for pagination"
    }
  },
  "required": [
    "PCID"
  ]
}
```
</Expandable>
