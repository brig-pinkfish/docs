---
title: "findymail"
sidebarTitle: "findymail"
description: "Findymail - find and verify email addresses, enrich company data, discover leads and employees"
---

{/* Auto-generated by scripts/generate-application-mcp-docs.ts — do not edit manually. To regenerate: see docs/scripts/README.md */}

**Server path:** `/dynamic/findymail` | **Type:** Application | **PCID required:** Yes

## Tools

| Tool | Description |
| --- | --- |
| [`findymail_find_email_by_name`](#findymail_find_email_by_name) | Find a work email address by person name and company domain |
| [`findymail_find_employees`](#findymail_find_employees) | Find employees at a company by job title |
| [`findymail_find_phone_number`](#findymail_find_phone_number) | Find a phone number from a LinkedIn profile URL |
| [`findymail_lookup_company`](#findymail_lookup_company) | Look up company data from a domain name |
| [`findymail_lookup_profile_by_email`](#findymail_lookup_profile_by_email) | Look up a person's profile from their email address |
| [`findymail_search_leads`](#findymail_search_leads) | Search for leads using natural language queries |
| [`findymail_verify_email`](#findymail_verify_email) | Verify if an email address is valid and deliverable |

---

## findymail_find_email_by_name

Find a work email address by person name and company domain

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "domain": {
      "type": "string",
      "description": "The company domain to search (e.g. \"example.com\")"
    },
    "name": {
      "type": "string",
      "description": "The full name of the person to find an email for"
    }
  },
  "required": ["PCID", "domain", "name"]
}
```

</Expandable>

---

## findymail_find_employees

Find employees at a company by job title

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "count": {
      "type": "integer",
      "description": "Maximum number of employees to return"
    },
    "job_titles": {
      "type": "array",
      "description": "List of job titles to search for (e.g. [\"CEO\", \"CTO\", \"VP of Engineering\"])"
    },
    "website": {
      "type": "string",
      "description": "The company website URL or domain (e.g. \"example.com\")"
    }
  },
  "required": ["PCID", "count", "job_titles", "website"]
}
```

</Expandable>

---

## findymail_find_phone_number

Find a phone number from a LinkedIn profile URL

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "linkedin_url": {
      "type": "string",
      "description": "The LinkedIn profile URL of the person (e.g. \"https://linkedin.com/in/johndoe\")"
    }
  },
  "required": ["PCID", "linkedin_url"]
}
```

</Expandable>

---

## findymail_lookup_company

Look up company data from a domain name

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "domain": {
      "type": "string",
      "description": "The company domain to enrich (e.g. \"example.com\")"
    }
  },
  "required": ["PCID", "domain"]
}
```

</Expandable>

---

## findymail_lookup_profile_by_email

Look up a person's profile from their email address

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "email": {
      "type": "string",
      "description": "The email address to look up"
    },
    "with_profile": {
      "type": "boolean",
      "description": "Whether to include full LinkedIn profile enrichment data (costs 2 credits instead of 1)"
    }
  },
  "required": ["PCID", "email"]
}
```

</Expandable>

---

## findymail_search_leads

Search for leads using natural language queries

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "config": {
      "type": "object",
      "description": "Configuration options for the search"
    },
    "limit": {
      "type": "integer",
      "description": "Maximum number of results to return"
    },
    "query": {
      "type": "string",
      "description": "Natural language search query describing the type of companies or leads to find (e.g. \"SaaS companies in San Francisco with 50-200 employees\")"
    }
  },
  "required": ["PCID", "query"]
}
```

</Expandable>

---

## findymail_verify_email

Verify if an email address is valid and deliverable

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID for the authenticated connection"
    },
    "email": {
      "type": "string",
      "description": "The email address to verify"
    }
  },
  "required": ["PCID", "email"]
}
```

</Expandable>
