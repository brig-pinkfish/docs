---
title: "google-ad-manager"
sidebarTitle: "google-ad-manager"
description: "Google Ad Manager API"
---

{/* Auto-generated by scripts/generate-application-mcp-docs.ts — do not edit manually. To regenerate: see docs/scripts/README.md */}

**Server path:** `/google-ad-manager` | **Type:** Application | **PCID required:** Yes

## Tools

| Tool | Description |
| --- | --- |
| [`google_ad_manager_create_report`](#google_ad_manager_create_report) | Create and run a report in Google Ad Manager |
| [`google_ad_manager_get_report`](#google_ad_manager_get_report) | Get the results of a previously created report |
| [`google_ad_manager_list_ad_units`](#google_ad_manager_list_ad_units) | List ad units in Google Ad Manager |
| [`google_ad_manager_get_ad_unit`](#google_ad_manager_get_ad_unit) | Get detailed information about a specific ad unit |
| [`google_ad_manager_list_orders`](#google_ad_manager_list_orders) | List orders in Google Ad Manager |
| [`google_ad_manager_get_order`](#google_ad_manager_get_order) | Get detailed information about a specific order |
| [`google_ad_manager_list_line_items`](#google_ad_manager_list_line_items) | List line items in Google Ad Manager |
| [`google_ad_manager_get_inventory`](#google_ad_manager_get_inventory) | Get inventory forecast for ad units |
| [`google_ad_manager_list_networks`](#google_ad_manager_list_networks) | List all Ad Manager networks accessible to the current authenticated user |

---

## google_ad_manager_create_report

Create and run a report in Google Ad Manager

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `networkCode` | string | Yes | — | The network code of the Ad Manager network |
| `reportName` | string | Yes | — | Name for the report |
| `dimensions` | string[] | Yes | — | Report dimensions to include |
| `metrics` | string[] | Yes | — | Report metrics to include |
| `startDate` | string | Yes | — | Report start date (YYYY-MM-DD format) |
| `endDate` | string | Yes | — | Report end date (YYYY-MM-DD format) |
| `dateRangeType` | string | No | `"CUSTOM"` | Date range type for the report |
| `adUnitView` | string | No | `"TOP_LEVEL"` | Ad unit view type |
| `timeZoneType` | string | No | `"PUBLISHER"` | Time zone for the report |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "networkCode": {
      "type": "string",
      "description": "The network code of the Ad Manager network"
    },
    "reportName": {
      "type": "string",
      "description": "Name for the report"
    },
    "dimensions": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "DATE",
          "MONTH",
          "WEEK",
          "AD_UNIT_NAME",
          "ADVERTISER_NAME",
          "ORDER_NAME",
          "LINE_ITEM_NAME",
          "CREATIVE_NAME",
          "PLACEMENT_NAME",
          "COUNTRY_NAME",
          "DEVICE_CATEGORY_NAME"
        ]
      },
      "description": "Report dimensions to include"
    },
    "metrics": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "TOTAL_IMPRESSIONS",
          "TOTAL_CLICKS",
          "TOTAL_CTR",
          "TOTAL_REVENUE",
          "TOTAL_FILL_RATE",
          "TOTAL_VIEWABLE_IMPRESSIONS",
          "TOTAL_MEASURABLE_IMPRESSIONS",
          "TOTAL_VIEWABILITY_RATE"
        ]
      },
      "description": "Report metrics to include"
    },
    "startDate": {
      "type": "string",
      "description": "Report start date (YYYY-MM-DD format)"
    },
    "endDate": {
      "type": "string",
      "description": "Report end date (YYYY-MM-DD format)"
    },
    "dateRangeType": {
      "type": "string",
      "enum": [
        "TODAY",
        "YESTERDAY",
        "LAST_WEEK",
        "LAST_MONTH",
        "CUSTOM"
      ],
      "default": "CUSTOM",
      "description": "Date range type for the report"
    },
    "adUnitView": {
      "type": "string",
      "enum": [
        "TOP_LEVEL",
        "FLAT",
        "HIERARCHICAL"
      ],
      "default": "TOP_LEVEL",
      "description": "Ad unit view type"
    },
    "timeZoneType": {
      "type": "string",
      "enum": [
        "PUBLISHER",
        "PROPOSAL_LOCAL"
      ],
      "default": "PUBLISHER",
      "description": "Time zone for the report"
    }
  },
  "required": [
    "PCID",
    "networkCode",
    "reportName",
    "dimensions",
    "metrics",
    "startDate",
    "endDate"
  ]
}
```

</Expandable>

---

## google_ad_manager_get_report

Get the results of a previously created report

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `networkCode` | string | Yes | — | The network code of the Ad Manager network |
| `reportJobId` | string | Yes | — | Report job ID from create report response |
| `format` | string | No | `"CSV"` | Export format for the report |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "networkCode": {
      "type": "string",
      "description": "The network code of the Ad Manager network"
    },
    "reportJobId": {
      "type": "string",
      "description": "Report job ID from create report response"
    },
    "format": {
      "type": "string",
      "enum": [
        "CSV",
        "TSV",
        "XML"
      ],
      "default": "CSV",
      "description": "Export format for the report"
    }
  },
  "required": [
    "PCID",
    "networkCode",
    "reportJobId"
  ]
}
```

</Expandable>

---

## google_ad_manager_list_ad_units

List ad units in Google Ad Manager

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `networkCode` | string | Yes | — | The network code of the Ad Manager network |
| `parentId` | string | No | — | Parent ad unit ID to filter by |
| `includeInactive` | boolean | No | `false` | Include inactive ad units |
| `limit` | number | No | `500` | Maximum number of ad units to return |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "networkCode": {
      "type": "string",
      "description": "The network code of the Ad Manager network"
    },
    "parentId": {
      "type": "string",
      "description": "Parent ad unit ID to filter by"
    },
    "includeInactive": {
      "type": "boolean",
      "default": false,
      "description": "Include inactive ad units"
    },
    "limit": {
      "type": "number",
      "default": 500,
      "description": "Maximum number of ad units to return"
    }
  },
  "required": [
    "PCID",
    "networkCode"
  ]
}
```

</Expandable>

---

## google_ad_manager_get_ad_unit

Get detailed information about a specific ad unit

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `networkCode` | string | Yes | — | The network code of the Ad Manager network |
| `adUnitId` | string | Yes | — | Ad unit ID to retrieve |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "networkCode": {
      "type": "string",
      "description": "The network code of the Ad Manager network"
    },
    "adUnitId": {
      "type": "string",
      "description": "Ad unit ID to retrieve"
    }
  },
  "required": [
    "PCID",
    "networkCode",
    "adUnitId"
  ]
}
```

</Expandable>

---

## google_ad_manager_list_orders

List orders in Google Ad Manager

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `networkCode` | string | Yes | — | The network code of the Ad Manager network |
| `advertiserId` | string | No | — | Filter by advertiser ID |
| `status` | string | No | — | Filter by order status |
| `limit` | number | No | `500` | Maximum number of orders to return |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "networkCode": {
      "type": "string",
      "description": "The network code of the Ad Manager network"
    },
    "advertiserId": {
      "type": "string",
      "description": "Filter by advertiser ID"
    },
    "status": {
      "type": "string",
      "enum": [
        "DRAFT",
        "PENDING_APPROVAL",
        "APPROVED",
        "DISAPPROVED",
        "PAUSED",
        "CANCELED"
      ],
      "description": "Filter by order status"
    },
    "limit": {
      "type": "number",
      "default": 500,
      "description": "Maximum number of orders to return"
    }
  },
  "required": [
    "PCID",
    "networkCode"
  ]
}
```

</Expandable>

---

## google_ad_manager_get_order

Get detailed information about a specific order

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `networkCode` | string | Yes | — | The network code of the Ad Manager network |
| `orderId` | string | Yes | — | Order ID to retrieve |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "networkCode": {
      "type": "string",
      "description": "The network code of the Ad Manager network"
    },
    "orderId": {
      "type": "string",
      "description": "Order ID to retrieve"
    }
  },
  "required": [
    "PCID",
    "networkCode",
    "orderId"
  ]
}
```

</Expandable>

---

## google_ad_manager_list_line_items

List line items in Google Ad Manager

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `networkCode` | string | Yes | — | The network code of the Ad Manager network |
| `orderId` | string | No | — | Filter by order ID |
| `status` | string | No | — | Filter by line item status |
| `limit` | number | No | `500` | Maximum number of line items to return |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "networkCode": {
      "type": "string",
      "description": "The network code of the Ad Manager network"
    },
    "orderId": {
      "type": "string",
      "description": "Filter by order ID"
    },
    "status": {
      "type": "string",
      "enum": [
        "DRAFT",
        "PENDING_APPROVAL",
        "APPROVED",
        "COMPLETED",
        "CANCELED",
        "PAUSED"
      ],
      "description": "Filter by line item status"
    },
    "limit": {
      "type": "number",
      "default": 500,
      "description": "Maximum number of line items to return"
    }
  },
  "required": [
    "PCID",
    "networkCode"
  ]
}
```

</Expandable>

---

## google_ad_manager_get_inventory

Get inventory forecast for ad units

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `networkCode` | string | Yes | — | The network code of the Ad Manager network |
| `adUnitIds` | string[] | Yes | — | Array of ad unit IDs to get inventory for |
| `startDate` | string | Yes | — | Forecast start date (YYYY-MM-DD format) |
| `endDate` | string | Yes | — | Forecast end date (YYYY-MM-DD format) |
| `targeting` | object | No | — | Targeting criteria for the forecast |

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "networkCode": {
      "type": "string",
      "description": "The network code of the Ad Manager network"
    },
    "adUnitIds": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of ad unit IDs to get inventory for"
    },
    "startDate": {
      "type": "string",
      "description": "Forecast start date (YYYY-MM-DD format)"
    },
    "endDate": {
      "type": "string",
      "description": "Forecast end date (YYYY-MM-DD format)"
    },
    "targeting": {
      "type": "object",
      "additionalProperties": true,
      "description": "Targeting criteria for the forecast"
    }
  },
  "required": [
    "PCID",
    "networkCode",
    "adUnitIds",
    "startDate",
    "endDate"
  ]
}
```

</Expandable>

---

## google_ad_manager_list_networks

List all Ad Manager networks accessible to the current authenticated user

<Expandable title="inputSchema">

```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    }
  },
  "required": [
    "PCID"
  ]
}
```

</Expandable>
