---
title: "google-task"
sidebarTitle: "google-task"
description: "Task lists and task management"
---

{/* Auto-generated by scripts/generate-application-mcp-docs.ts — do not edit manually */}

**Server path:** `/google-task` | **Type:** Application | **PCID required:** Yes

Task lists and task management

## Tools

| Tool | Description |
| --- | --- |
| [`google-task_list_tasks`](#google-task_list_tasks) | List tasks from Google Tasks with optional pagination |
| [`google-task_get_task`](#google-task_get_task) | Retrieve a specific task from Google Tasks by its ID |
| [`google-task_create_task`](#google-task_create_task) | Create a new task in Google Tasks |
| [`google-task_update_task`](#google-task_update_task) | Update an existing task in Google Tasks |
| [`google-task_delete_task`](#google-task_delete_task) | Delete a task from Google Tasks |
| [`google-task_clear_tasks`](#google-task_clear_tasks) | Clear completed tasks from a Google Tasks list |
| [`google-task_search_tasks`](#google-task_search_tasks) | Search for tasks in Google Tasks using a query string |

---

## google-task_list_tasks

List tasks from Google Tasks with optional pagination

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `taskListId` | string | No | — | Task list ID to list tasks from |
| `cursor` | string | No | — | Cursor to paginate through tasks |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "taskListId": {
      "type": "string",
      "description": "Task list ID to list tasks from"
    },
    "cursor": {
      "type": "string",
      "description": "Cursor to paginate through tasks"
    }
  },
  "required": [
    "PCID"
  ]
}
```
</Expandable>

---

## google-task_get_task

Retrieve a specific task from Google Tasks by its ID

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `taskId` | string | Yes | — | Task ID to retrieve |
| `taskListId` | string | No | — | Task list ID to retrieve the task from |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "taskId": {
      "type": "string",
      "description": "Task ID to retrieve"
    },
    "taskListId": {
      "type": "string",
      "description": "Task list ID to retrieve the task from"
    }
  },
  "required": [
    "PCID",
    "taskId"
  ]
}
```
</Expandable>

---

## google-task_create_task

Create a new task in Google Tasks

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `taskListId` | string | No | — | Task list ID to create the task in |
| `title` | string | Yes | — | Title of the task |
| `notes` | string | No | — | Notes of the task |
| `due` | string | No | — | Due date of the task. Accepts simple date (YYYY-MM-DD) or full ISO 8601 format (YYYY-MM-DDTHH:mm:ss.sssZ) |
| `status` | string | No | — | Status of the task |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "taskListId": {
      "type": "string",
      "description": "Task list ID to create the task in"
    },
    "title": {
      "type": "string",
      "description": "Title of the task"
    },
    "notes": {
      "type": "string",
      "description": "Notes of the task"
    },
    "due": {
      "type": "string",
      "description": "Due date of the task. Accepts simple date (YYYY-MM-DD) or full ISO 8601 format (YYYY-MM-DDTHH:mm:ss.sssZ)"
    },
    "status": {
      "type": "string",
      "enum": [
        "needsAction",
        "completed"
      ],
      "description": "Status of the task"
    }
  },
  "required": [
    "PCID",
    "title"
  ]
}
```
</Expandable>

---

## google-task_update_task

Update an existing task in Google Tasks

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `taskId` | string | Yes | — | Task ID to update |
| `taskListId` | string | No | — | Task list ID to update the task in |
| `title` | string | No | — | Title of the task |
| `notes` | string | No | — | Notes of the task |
| `due` | string | No | — | Due date of the task. Accepts simple date (YYYY-MM-DD) or full ISO 8601 format (YYYY-MM-DDTHH:mm:ss.sssZ) |
| `status` | string | No | — | Status of the task |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "taskId": {
      "type": "string",
      "description": "Task ID to update"
    },
    "taskListId": {
      "type": "string",
      "description": "Task list ID to update the task in"
    },
    "title": {
      "type": "string",
      "description": "Title of the task"
    },
    "notes": {
      "type": "string",
      "description": "Notes of the task"
    },
    "due": {
      "type": "string",
      "description": "Due date of the task. Accepts simple date (YYYY-MM-DD) or full ISO 8601 format (YYYY-MM-DDTHH:mm:ss.sssZ)"
    },
    "status": {
      "type": "string",
      "enum": [
        "needsAction",
        "completed"
      ],
      "description": "Status of the task"
    }
  },
  "required": [
    "PCID",
    "taskId"
  ]
}
```
</Expandable>

---

## google-task_delete_task

Delete a task from Google Tasks

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `taskId` | string | Yes | — | Task ID to delete |
| `taskListId` | string | No | — | Task list ID to delete the task from |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "taskId": {
      "type": "string",
      "description": "Task ID to delete"
    },
    "taskListId": {
      "type": "string",
      "description": "Task list ID to delete the task from"
    }
  },
  "required": [
    "PCID",
    "taskId"
  ]
}
```
</Expandable>

---

## google-task_clear_tasks

Clear completed tasks from a Google Tasks list

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `taskListId` | string | Yes | — | Task list ID to clear completed tasks from |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "taskListId": {
      "type": "string",
      "description": "Task list ID to clear completed tasks from"
    }
  },
  "required": [
    "PCID",
    "taskListId"
  ]
}
```
</Expandable>

---

## google-task_search_tasks

Search for tasks in Google Tasks using a query string

**Parameters:**

| Parameter | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `query` | string | Yes | — | Query to search for tasks |

<Expandable title="inputSchema">
```json
{
  "type": "object",
  "properties": {
    "PCID": {
      "type": "string",
      "description": "Pink Connect ID"
    },
    "query": {
      "type": "string",
      "description": "Query to search for tasks"
    }
  },
  "required": [
    "PCID",
    "query"
  ]
}
```
</Expandable>
