---
title: "Connecting to MCPs"
sidebarTitle: "Connecting to MCPs"
description: "PCIDs and external integrations"
---

Two server types: **embedded** (no connection) and **application** (Gmail, Slack, etc. — require PCID per call). Set up connections on the [Integrations](https://app.pinkfish.ai/integrations) page in the Pinkfish web app. Get PCIDs from `capabilities_discover` or the [PinkConnect API](#listing-connections).

## Embedded MCPs

Built-in services. No PCID required.

| Server                   | Description                                                              |
| ------------------------ | ------------------------------------------------------------------------ |
| `datastore-structured`   | Tables with schemas — structured data storage                            |
| `datastore-unstructured` | Flexible key-value JSON storage                                          |
| `filestorage`            | File upload, download, and management                                    |
| `knowledge-base`         | RAG search and semantic Q&A over documents                               |
| `vault`                  | Encrypted secrets storage                                                |
| `web-search`             | Google search, news, YouTube, shopping, and more                         |
| `web-scraping`           | Extract content from URLs, crawl websites                                |
| `image-processing`       | AI image generation and editing                                          |
| `video-processing`       | AI video generation                                                      |
| `audio-processing`       | Text-to-speech and transcription                                         |
| `browser-automation`     | AI-powered browser automation                                            |
| `code-execution`         | Sandboxed JavaScript execution                                           |
| `embedded-anthropic`     | Claude models                                                            |
| `embedded-openai`        | OpenAI GPT models                                                        |
| `embedded-gemini`        | Google Gemini models                                                     |
| `embedded-groq`          | Groq models                                                              |
| `embedded-perplexity`    | Perplexity web research                                                  |
| `docprocess`             | PDF, Word, and document conversion                                       |
| `email`                  | Send emails                                                              |
| `charting`               | Chart.js visualizations                                                  |
| `http-utils`             | Make HTTP requests to public APIs                                        |
| `weather`                | Current conditions and forecasts                                         |
| `ocr`                    | Optical character recognition                                            |
| `web-application`        | Deploy HTML dashboards to shareable URLs                                 |
| `pinkfish-sidekick`      | Pinkfish Workflow Building — discovery, workflow building, orchestration |

## Application MCPs

OAuth/API key integrations. Require a connection in PinkConnect and a PCID per tool call.

| Server              | Description                |
| ------------------- | -------------------------- |
| `gmail`             | Gmail API                  |
| `slack`             | Slack API                  |
| `salesforce`        | Salesforce API             |
| `google-drive`      | Google Drive file storage  |
| `google-sheets`     | Google Sheets API          |
| `google-calendar`   | Google Calendar API        |
| `onedrive`          | OneDrive API               |
| `microsoft-outlook` | Outlook email and calendar |
| `microsoft-teams`   | Microsoft Teams API        |
| `sharepoint`        | SharePoint API             |
| `hubspot`           | HubSpot CRM                |
| `jira`              | Jira API                   |
| `linear`            | Linear API                 |
| `github`            | GitHub API                 |
| `snowflake`         | Snowflake data warehouse   |
| `notion`            | Notion API                 |
| ...                 | 100+ more integrations     |

## PCID

A **PCID** (PinkConnect ID) identifies a specific OAuth connection. Include it in tool arguments so the MCP Farm knows which connection to use (e.g. which Gmail account).

## Listing Connections

### PinkConnect API

**Base URL:** `https://proxy.pinkfish.ai`

```bash
curl -s -X GET "https://proxy.pinkfish.ai/manage/user-connections" \
  -H "Authorization: Bearer $PINKFISH_TOKEN" \
  -H "Content-Type: application/json"
```

| Query param | Description                                                          |
| ----------- | -------------------------------------------------------------------- |
| `statuses`  | Comma-separated: `connected`, `error`, `disconnected`. Default: all. |

**Response:** Array of connections. Each `id` is the PCID.

```json
[
  {
    "id": "abc123-def456",
    "name": "My Gmail",
    "service_key": "gmail",
    "status": "connected"
  },
  {
    "id": "ghi789-jkl012",
    "name": "My Slack",
    "service_key": "slack",
    "status": "connected"
  }
]
```

### Discovery

Use `capabilities_discover` for task-relevant connections. PCIDs are in `connections[].id`:

```bash
curl -s -X POST "https://mcp.app.pinkfish.ai/pinkfish-sidekick" \
  -H "Authorization: Bearer $PINKFISH_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "capabilities_discover",
      "arguments": {
        "request": "search my gmail"
      }
    },
    "id": 1
  }'
```

## Using a PCID

Pass the PCID as an argument when calling an application MCP tool:

```bash
curl -s -X POST "https://mcp.app.pinkfish.ai/gmail" \
  -H "Authorization: Bearer $PINKFISH_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "gmail_search_emails",
      "arguments": {
        "PCID": "abc123-def456",
        "query": "is:unread"
      }
    },
    "id": 1
  }'
```
